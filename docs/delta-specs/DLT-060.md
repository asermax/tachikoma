# Delta Spec: DLT-060 - Check for agent updates and notify user

## Story

As a Tachikoma operator, I want to be notified when a newer version of tachikoma-agent is available on PyPI so that I can stay current with bug fixes and features without manually checking for releases.

## What It Does

A scheduled job periodically queries the PyPI JSON API for the latest version of `tachikoma-agent` and compares it against the currently installed version. When a newer version is found, a notification is dispatched to the user through the active channel. The user sees a message indicating what version is available and can act on it at their convenience — the actual update application is handled by DLT-061.

## Requirements

| ID | Requirement | Status |
|----|-------------|--------|
| R0 | Periodically check PyPI for newer versions of tachikoma-agent | Core goal |
| R1 | Notify the user via direct notification when a newer version is available | Must-have |
| R2 | Configurable check frequency with a sensible default (once per day) | Must-have |
| R3 | Silent when already on the latest version — no notification, no log noise | Must-have |
| R4 | Only notify once per new version — no repeated notifications for the same available version | Must-have |
| R5 | Manual trigger: agent can check for updates on demand (e.g., user asks "any updates?") | Nice-to-have |

## Acceptance Criteria

### Periodic version check (R0, R2)

- [ ] Given the scheduler is running, when the configured interval elapses, then the system queries PyPI for the latest version of `tachikoma-agent`
- [ ] Given the `[updates]` config section is absent, when the application starts, then update checking is enabled by default with a frequency of once per day (86400 seconds)
- [ ] Given the `[updates]` config section specifies a custom interval, when the scheduler runs, then checks occur at that interval

### Update notification (R1)

- [ ] Given a PyPI check finds a version newer than the currently installed one, when the check completes, then a notification is dispatched through the event bus at Normal priority (using the existing priority buffer for delivery)
- [ ] Given the notification is delivered, when the user sees it, then it contains the available version and the current version
- [ ] Given a notification was dispatched but no channel is active before shutdown, when the application restarts, then the dedup state persists so the version is not re-notified (the notification itself is ephemeral — it is not re-delivered)

### Silent on latest (R3)

- [ ] Given a PyPI check finds the installed version matches the latest stable release on PyPI, when the check completes, then no notification is dispatched
- [ ] Given the installed version is higher than the latest stable release on PyPI (e.g., dev build), when a check completes, then no notification is dispatched
- [ ] Given multiple checks occur while already on the latest, when each completes, then no log output beyond debug-level is produced

### One notification per version (R4)

- [ ] Given a notification was already dispatched for version X, when a subsequent check still finds X as the latest, then no additional notification is dispatched
- [ ] Given a notification was dispatched for version X, when a newer version Y becomes available, then a new notification is dispatched for Y
- [ ] Given no notification dedup state exists (first run after installation), when a check finds a newer version available, then a notification is dispatched

### Version filtering

- [ ] Given the PyPI response contains pre-release or development versions, when comparing versions, then only stable releases are considered for update notification

### Error handling

- [ ] Given the PyPI API is unreachable, when a check is attempted, then the error is logged at warning level and no notification is dispatched — the next scheduled check retries
- [ ] Given the PyPI response is malformed or missing version data, when the response is parsed, then the error is logged at warning level and no notification is dispatched

### Manual trigger (R5)

- [ ] Given the agent is asked to check for updates, when the MCP tool is invoked, then a version check runs immediately (bypassing the schedule) and the result is returned as structured data: current version, latest version, and whether an update is available

## Requires

Dependencies:
- None

---

## Detected Impacts

### Affected Features
- **docs/feature-specs/configuration/config-system.md** - Adds: `[updates]` configuration section for check interval and enabled flag
- **docs/feature-specs/distribution/release-pipeline.md** - Adds: update-check capability that consumes the PyPI publishing output

### Notes for Reconciliation
- Config system feature spec needs a new behavior section for `[updates]` configuration
- Distribution feature spec may reference update checking as a downstream consumer of the release pipeline
