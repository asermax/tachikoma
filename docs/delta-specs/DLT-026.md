# Delta Spec: DLT-026 - Detect conversation boundaries via topic analysis

## Story

As a user, I want the assistant to detect when I've changed topics so that each conversation is processed separately — memories are extracted from the completed conversation, and the new topic starts fresh without irrelevant prior context bleeding in.

## What It Does

On every incoming message, a lightweight boundary detector compares the message against a rolling summary of the current conversation. If the message continues the current topic, processing proceeds normally. If it detects a topic shift, the system closes the current session (triggering memory extraction asynchronously), starts a fresh SDK conversation so prior context doesn't bleed in, opens a new session with a short summary of the previous conversation injected into the system prompt, and then processes the message. A per-message post-processing pipeline maintains the rolling conversation summary after each agent response, keeping it ready for the next boundary check.

## Requirements

| ID | Requirement | Status |
|----|-------------|--------|
| R0 | Detect whether an incoming message continues the current conversation or starts a new one | Core goal |
| R1 | On topic shift, close the current session and open a new one before the coordinator processes the message | Must-have |
| R2 | On session close via boundary detection, trigger the session post-processing pipeline asynchronously so it doesn't block the new session | Must-have |
| R3 | Per-message post-processing pipeline (reusing existing `PostProcessingPipeline` infrastructure) runs asynchronously after each agent response | Must-have |
| R3.1 | Conversation summary processor: generates or updates a concise rolling summary of the conversation after each agent response | Must-have |
| R4 | Store a rolling conversation summary on the session record, updated by the per-message pipeline | Must-have |
| R5 | Boundary detector uses the current session summary for comparison against the incoming message | Must-have |
| R6 | Boundary detection call adds no more than 1-2 seconds to message processing latency (excludes await-pending time from R11) | Must-have |
| R7 | Handle edge cases: first message with no prior session (skip detection), detector errors, ambiguous shifts, messages arriving during transition | Must-have |
| R8 | Boundary detection failures must not block or crash message processing (graceful degradation, fail-open to continuation) | Must-have |
| R9 | After a topic shift, the new conversation must start without prior conversation context from the SDK session | Must-have |
| R10 | Inject the previous conversation summary into the new session's system prompt so the agent has brief context of what came before | Must-have |
| R11 | When a new message arrives, await any pending per-message post-processing before proceeding to ensure the summary is up-to-date for boundary detection | Must-have |
| R12 | Boundary detection must operate independently of the active conversation session — it must not interfere with or depend on the coordinator's SDK session state | Must-have |
| R13 | Rolling conversation summary must remain concise and bounded in size regardless of conversation length | Must-have |
| R14 | On graceful shutdown, await any running background post-processing tasks before exiting | Must-have |

## Acceptance Criteria

### Boundary Detection (R0, R5, R6, R12)

- [ ] Given an active session with a conversation summary, when a new message arrives that continues the same topic, then the boundary detector classifies it as a continuation and the message proceeds to the coordinator normally
- [ ] Given an active session with a conversation summary, when a new message arrives on a clearly different topic, then the boundary detector classifies it as a topic shift
- [ ] Given a boundary detection call, when the detector runs, then it operates independently of the active SDK conversation session
- [ ] Given a boundary detection call, when the detector completes, then the detection itself adds no more than 1-2 seconds to message processing latency (excluding any await-pending time)

### Session Transition on Topic Shift (R1, R9, R10)

- [ ] Given the boundary detector identifies a topic shift, when the transition begins, then the current session is closed in the session registry before the new session is created
- [ ] Given a topic shift is detected, when the new conversation starts, then the SDK conversation context is reset so no prior context bleeds into the new session
- [ ] Given a topic shift is detected, when the new session starts, then the previous conversation's summary is injected into the system prompt for the new SDK session
- [ ] Given the session transition completes, when the coordinator processes the incoming message, then it does so within the new session context
- [ ] Given a session transition is in progress, when the transition completes, then the entire boundary-check-to-transition sequence is atomic from the channel's perspective — the channel's next `send_message()` call waits for it to finish

### Async Session Post-Processing (R2, R14)

- [ ] Given the current session is closed due to a topic shift, when the session has a valid SDK session ID, then the session-level post-processing pipeline is triggered asynchronously (does not block the new session or the incoming message)
- [ ] Given the current session is closed due to a topic shift, when the session has no SDK session ID (e.g., no agent response yet), then session post-processing is skipped
- [ ] Given async session post-processing is running from a previous topic shift, when the system operates normally, then the background task completes without affecting active conversations
- [ ] Given async session post-processing fails, when the error occurs, then it is logged but does not affect the active conversation
- [ ] Given multiple topic shifts occur in sequence, when background post-processing tasks accumulate, then all tasks are tracked and complete independently
- [ ] Given the system is shutting down gracefully, when background post-processing tasks are still running, then shutdown awaits their completion before exiting

### Per-Message Post-Processing (R3, R3.1, R4, R13)

- [ ] Given the agent completes a response, when the response stream ends, then the per-message post-processing pipeline is triggered asynchronously
- [ ] Given the per-message pipeline runs, when the summary processor executes, then it generates or updates a rolling conversation summary and stores it on the session record
- [ ] Given the per-message pipeline uses the existing `PostProcessingPipeline` infrastructure, when processors are registered, then they follow the same interface and error isolation patterns as session-level processors
- [ ] Given a per-message processor fails, when the error occurs, then it is logged and the conversation continues uninterrupted
- [ ] Given a per-message processor failed on a previous exchange, when the next agent response completes, then the per-message pipeline runs again (each invocation is independent — no permanent failure state)
- [ ] Given a long conversation (many exchanges), when the summary processor runs, then the resulting summary remains concise and bounded in size

### Await Pending Post-Processing (R11)

- [ ] Given per-message post-processing from a previous response is still running, when a new message arrives, then the system awaits its completion before proceeding to boundary detection
- [ ] Given no per-message post-processing is pending, when a new message arrives, then boundary detection proceeds immediately with no delay
- [ ] Given the pending per-message post-processing fails while being awaited, when the error occurs, then it is logged and message processing continues (boundary detection runs with whatever summary is available)

### Edge Cases and Graceful Degradation (R7, R8)

- [ ] Given no active session exists (first message ever, or after startup), when a message arrives, then boundary detection is skipped and a new session is created normally
- [ ] Given an active session with no summary yet (first message in session, per-message pipeline hasn't run), when the next message arrives, then boundary detection is skipped and the message proceeds normally
- [ ] Given the boundary detector encounters an error (SDK failure, timeout, malformed response), when the error occurs, then it is logged and the message proceeds as a continuation (fail-open)
- [ ] Given the SDK conversation context reset fails during a topic shift, when the error occurs, then the error is logged, a new Tachikoma session is still created, and the message is processed — the agent may have stale context but the conversation is not blocked

## Requires

Dependencies:
- None (extends existing session tracking and post-processing pipeline capabilities already in the codebase)

---

## Detected Impacts

### Affected Features
- **docs/feature-specs/agent/core-architecture.md** — Modifies: Coordinator's `send_message()` gains boundary detection gating, per-message post-processing trigger, await-pending logic, and SDK session reset. The `__aexit__` shutdown flow changes to await background post-processing tasks. Coordinator constructor accepts additional dependencies (per-message pipeline, boundary detector).
- **docs/feature-specs/agent/sessions.md** — Modifies: Session model gains a `summary` field for the rolling conversation summary. Sessions can now close mid-conversation (boundary detection), not just on shutdown or crash recovery.
- **docs/feature-specs/agent/post-processing-pipeline.md** — Modifies: Pipeline is now used in two contexts — session-level (existing, triggered on session close) and per-message (new, triggered after each agent response). The pipeline class itself doesn't change, but its usage pattern expands.

### Notes for Reconciliation
- Core architecture spec needs updated data flow for send_message() (await pending → boundary check → process → per-message post-processing)
- Core architecture spec needs updated shutdown flow (await background post-processing tasks)
- Sessions spec needs new requirement for the summary field and mid-conversation session close
- Post-processing pipeline spec may need a note about the two usage contexts
- A new feature spec may be needed for "conversation boundary detection" under `docs/feature-specs/agent/`
