# Delta Spec: DLT-030 - Manage external project repositories

## Story

As a user, I want the assistant to manage external code repositories as git submodules so that I can ask it to track, modify, and contribute to multiple codebases alongside its own workspace.

## What It Does

Enables the assistant to manage external code repositories as git submodules within a dedicated `projects/` directory in the workspace. Projects are registered during conversations via an MCP tool, synced on every startup, and automatically committed and pushed at session end — before the main workspace commit — so that submodule references stay in sync. A context provider injects project awareness at session start so the agent knows what's available.

Git authentication (SSH keys, personal access tokens, etc.) is the user's responsibility — the system assumes credentials are configured externally and reports clear errors on authentication failure.

## Requirements

| ID | Requirement | Status |
|----|-------------|--------|
| R0 | Manage external code repositories as git submodules for multi-codebase workflows | Core goal |
| R1 | Register new projects by name and git URL during conversations (MCP tool); submodule is checked out to default branch (per remote HEAD reference), not detached HEAD | Must-have |
| R2 | Store projects as git submodules within a `projects/` directory in the workspace | Must-have |
| R3 | On startup, initialize and pull all submodules in parallel (1 retry on failure, log and continue) | Must-have |
| R3.1 | After pull, check out the default branch (per remote HEAD reference, not detached HEAD) | Must-have |
| R4 | At session end, commit and push changes in each submodule before the main workspace commit | Must-have |
| R4.1 | Generate descriptive commits for submodule changes using the same approach as workspace version tracking | Must-have |
| R4.2 | Push after commit; on push failure, log and continue | Must-have |
| R4.3 | Process multiple submodules without sequential bottleneck | Must-have |
| R5 | Main workspace git history tracks submodule reference updates (existing GitProcessor runs after projects processor) | Must-have |
| R6 | Inject project context at session start via pre-processing context provider | Must-have |
| R7 | Deregister/remove a project (MCP tool); warn and require confirmation when uncommitted changes exist | Must-have |

## Acceptance Criteria

### Project Registration (R1, R2)

- [ ] Given a conversation is active, when the agent calls the `register_project` tool with a name and git URL, then the repository is added as a git submodule under `projects/<name>` in the workspace
- [ ] Given a project is registered, when the submodule is added, then it is checked out to its default branch (determined by the remote's HEAD reference), not detached HEAD
- [ ] Given a freshly registered project, when the submodule is added, then it contains a clean checkout of the remote's default branch with no local modifications
- [ ] Given a project name that already exists in `projects/`, when `register_project` is called, then the tool returns an error indicating the project already exists
- [ ] Given an invalid git URL or unreachable remote, when `register_project` is called, then the tool returns an error with a clear message and no partial state is left behind
- [ ] Given a git URL requiring authentication the user has not configured, when `register_project` is called, then the tool returns an error indicating authentication failure
- [ ] Given a project is successfully registered, when the workspace git status is checked, then the `.gitmodules` file and `projects/<name>` entry are present as uncommitted changes (the next GitProcessor run will commit them)

### Startup Sync (R3, R3.1)

- [ ] Given registered submodules exist in `.gitmodules`, when the application starts, then all submodules are initialized and pulled in parallel
- [ ] Given a submodule pull fails, when the first attempt fails, then it retries once before logging the failure and continuing with other submodules
- [ ] Given a submodule pull succeeds, when the pull completes, then the submodule is checked out to its default branch (determined by remote's HEAD reference, not left in detached HEAD)
- [ ] Given a submodule has unpushed local commits that conflict with remote changes, when the startup pull runs, then the pull failure is logged with details about the conflict and the submodule is left in its pre-pull state
- [ ] Given no submodules are registered, when the bootstrap hook runs, then it completes as a no-op after creating the `projects/` directory
- [ ] Given some submodules fail to pull after retry, when the bootstrap completes, then the application starts normally with successful submodules available and failures logged
- [ ] Given the `projects/` directory already exists, when the bootstrap hook runs, then it does not recreate it (idempotent)
- [ ] Given a submodule's remote requires authentication the user has not configured, when the pull fails, then the failure message indicates authentication failure

### Session-End Commit and Push (R4, R4.1, R4.2, R4.3, R5)

- [ ] Given uncommitted changes exist in a submodule, when the projects post-processor runs, then descriptive commits are generated for the submodule's changes (using the same approach as workspace version tracking)
- [ ] Given multiple submodules have uncommitted changes, when the projects post-processor runs, then all submodules are processed in parallel
- [ ] Given a submodule has commits to push, when commits complete, then the processor pushes to the submodule's remote
- [ ] Given a push fails for a submodule, when the error occurs, then it is logged and other submodules continue processing normally
- [ ] Given a submodule's remote has advanced since last pull, when push fails with non-fast-forward, then the failure is logged with a message indicating the remote has diverged (changes remain committed locally and will be reconciled on next startup pull)
- [ ] Given the projects post-processor completes (with or without errors), when the existing GitProcessor runs next, then it sees the updated submodule references and commits them as part of the workspace history
- [ ] Given no submodules have uncommitted changes, when the projects post-processor runs, then it completes as a no-op without spawning any agents
- [ ] Given the commit generation fails for a submodule, when the error occurs, then it is logged and the processor continues with other submodules (no push attempted for the failed one)

### Project Context Injection (R6)

- [ ] Given registered projects exist, when a new session starts, then the pre-processing context provider injects a context block listing all projects with their names, paths, and current branch
- [ ] Given no projects are registered, when a new session starts, then the context provider returns no context (does not inject an empty block)
- [ ] Given a project's directory is missing or corrupted, when the context provider runs, then it logs a warning and excludes that project from the context block
- [ ] Given a project's submodule is in detached HEAD state, when the context provider runs, then it reports the commit hash instead of a branch name

### Project Deregistration (R7)

- [ ] Given a registered project with no uncommitted changes, when the agent calls the `deregister_project` tool with the project name, then the submodule is fully removed (deinit, remove from `.gitmodules`, remove directory)
- [ ] Given a registered project with uncommitted local changes, when `deregister_project` is called without the force flag, then the tool returns a warning listing the uncommitted changes and requires the `force` flag to proceed
- [ ] Given a registered project with uncommitted local changes, when `deregister_project` is called with the force flag, then the submodule is removed and the changes are lost
- [ ] Given a project name that does not exist, when `deregister_project` is called, then the tool returns an error indicating the project was not found
- [ ] Given a project is deregistered, when the workspace git status is checked, then the removal changes are present as uncommitted changes (the next GitProcessor run will commit them)

## Requires

Dependencies:
- None (builds on existing features: workspace-bootstrap, workspace-version-tracking, post-processing-pipeline, pre-processing-pipeline)

---

## Detected Impacts

### Affected Features
- **docs/feature-specs/agent/workspace-bootstrap.md** - Adds: new projects bootstrap hook for submodule init/pull on startup
- **docs/feature-specs/agent/workspace-version-tracking.md** - Modifies: projects post-processor must run before git post-processor in finalize phase; git post-processor now also commits submodule reference changes
- **docs/feature-specs/agent/post-processing-pipeline.md** - Modifies: may require ordering support within finalize phase or a new intermediate phase to guarantee projects processor runs before git processor
- **docs/feature-specs/agent/pre-processing-pipeline.md** - Adds: new projects context provider registered with the pipeline
- **docs/feature-specs/agent/core-architecture.md** - Modifies: new hook, post-processor, pre-processor, and MCP tools registered at startup

### Notes for Reconciliation
- workspace-bootstrap: add projects hook to the registered hooks list and its acceptance criteria
- workspace-version-tracking: document that git post-processor now commits submodule reference updates alongside other workspace changes
- post-processing-pipeline: document ordering mechanism if finalize phase is extended
- pre-processing-pipeline: no structural changes (new provider follows existing extensibility pattern)
- core-architecture: add MCP server registration for project management tools
- New feature doc likely needed: `docs/feature-specs/agent/project-management.md`
