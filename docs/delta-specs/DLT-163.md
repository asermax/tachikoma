# Delta Spec: DLT-163 - Block message enqueue during exchange teardown

## Story

As a developer, I need the coordinator to guarantee that no user message is accepted by a session that cannot process it, so that messages arriving during exchange teardown are never silently lost or routed to a dead session.

## What It Does

When a message exchange completes, messages arriving in the gap between final response delivery and teardown can still be accepted by the message queue, landing in a session that has already finished processing. The Telegram channel has a drain loop intended to handle these leftover messages, but it doesn't work. This delta removes the broken drain loop from the Telegram channel and moves the responsibility into the coordinator: after exchange teardown, any leftover messages in the buffer are automatically re-processed in the same session with their original message ID, requiring no channel involvement.

## Requirements

| ID | Requirement | Status |
|----|-------------|--------|
| R0 | No user message is accepted by a session that cannot process it | Core goal |
| R1 | After exchange teardown, leftover messages are automatically re-processed in the same session with their original message ID | Must-have |
| R2 | Remove the broken Telegram drain loop | Must-have |
| R3 | The re-queue mechanism is owned by the coordinator, not channels | Must-have |
| R4 | No behavioral change when messages arrive during active processing (outside teardown window) | Must-have |
| R5 | The re-queue does not add latency to the happy path | Nice-to-have |

## Acceptance Criteria

### Exchange teardown guard (R0, R1)

- [ ] Given an exchange is in teardown (forwarder cancelled, client disconnecting), when a message arrives via `enqueue()`, then the message is preserved and not lost
- [ ] Given an exchange completes teardown with leftover messages in the buffer, when the coordinator checks for them, then each leftover message is re-processed in the same session via a new `send_message()` turn
- [ ] Given a leftover message is re-processed, when the coordinator starts the new turn, then it uses the same session (same `sdk_session_id`) as the completed exchange
- [ ] Given a leftover message is re-processed, when the response is streamed, then the channel can correlate it to the original message (message ID preserved)

### Telegram drain loop removal (R2)

- [ ] Given the Telegram channel's `_process_through_coordinator` method, when processing messages, then there is no `while has_pending_messages` drain loop
- [ ] Given the drain loop is removed, when messages arrive during active processing, then they are handled by the coordinator's re-queue mechanism instead

### Coordinator ownership (R3)

- [ ] Given the coordinator completes an exchange, when leftover messages exist in the buffer, then the coordinator automatically initiates re-processing without any channel action
- [ ] Given the coordinator has leftover messages, when re-processing starts, then channels receive the same `AsyncIterator[AgentEvent]` interface as a normal turn — no new channel-side logic needed

### Happy path preservation (R4)

- [ ] Given an exchange is actively processing (not in teardown), when a message arrives via `enqueue()`, then the forwarder routes it to the SDK inbox as before (steering message behavior unchanged)
- [ ] Given no messages arrive during teardown, when the exchange completes, then no re-processing occurs and behavior is identical to the current flow

## Requires

Dependencies: None

---

## Detected Impacts

### Affected Features
- **docs/feature-specs/agent/core-architecture.md** - Modifies: message buffer flow description and session lifecycle to reflect coordinator-owned re-queue
- **docs/feature-designs/agent/core-architecture.md** - Modifies: message buffer flow, coordinator state documentation, drain loop references
- **docs/feature-specs/channels/telegram.md** - Modifies: remove drain loop from Telegram channel description
- **docs/feature-designs/channels/telegram.md** - Modifies: remove drain loop from Telegram channel design

### Notes for Reconciliation
- Core architecture spec's "Message buffer flow" section needs updating to describe coordinator-owned re-queue instead of channel drain loop
- Core architecture design's data flow and component descriptions need updating for new coordinator behavior
- Telegram channel spec/design need drain loop references removed
- REPL channel is unaffected (no drain loop exists)
