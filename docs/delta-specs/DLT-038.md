# Delta Spec: DLT-038 - Hot-reload skills at runtime

## Story

As the assistant, I want the skill registry to reflect the current state of the skills directory so that skills I create or modify during execution become available for detection without requiring an application restart.

## What It Does

Adds a re-scan mechanism to the `SkillRegistry` so it refreshes its in-memory index from the filesystem each time the `SkillsContextProvider` runs detection on a new session. This ensures newly authored, updated, or deleted skills are picked up by the next session's detection cycle. The change is internal to the skills subsystem — no API changes to the provider or coordinator.

## Requirements

| ID | Requirement | Status |
|----|-------------|--------|
| R0 | Skills added or modified at runtime become available for detection without application restart | Core goal |
| R1 | Registry re-scans the skills directory to pick up additions, modifications, and deletions | Must-have |
| R2 | Change detection mechanism to avoid unnecessary re-scans when nothing changed | Must-have |
| R3 | Re-scan preserves existing error handling (invalid skills skipped, valid ones loaded) | Must-have |
| R4 | Re-scan is transparent to the context provider — no API change to `provide()` | Must-have |
| R5 | Deleted skill directories are removed from the registry after re-scan | Must-have |
| R6 | Mid-session skill changes do not affect the current session's detected skills (session stability invariant preserved) | Must-have |
| R7 | Re-scan does not block message processing with expensive I/O | Nice-to-have |

## Acceptance Criteria

### New skill available after creation (R0, R1)
- [ ] Given the registry has loaded, when a new skill directory with valid SKILL.md is created in `workspace/skills/`, then the next new session's `provide()` call discovers the new skill and includes it in classification
- [ ] Given a new skill with agents is created, when the registry re-scans, then the new skill's agents are available for detection and delegation

### Modified skill reflected after update (R0, R1)
- [ ] Given an existing skill's SKILL.md description is modified, when the registry re-scans, then the updated description is used for classification
- [ ] Given an existing skill's agent definitions are modified (added, removed, or changed), when the registry re-scans, then the updated agents are reflected in the registry

### Deleted skill removed (R5)
- [ ] Given a skill directory is deleted from `workspace/skills/`, when the registry re-scans, then the skill and its agents are no longer in the registry
- [ ] Given a deleted skill was previously detected in an active session, then the current session's detected skills are not affected (R6)
- [ ] Given the entire `workspace/skills/` directory is deleted, when the registry re-scans, then it gracefully empties itself (no skills, no agents)

### Change detection avoids unnecessary work (R2)
- [ ] Given no changes have occurred in the skills directory since the last scan, when the provider triggers a re-scan check, then the registry skips the full re-scan
- [ ] Given a change has occurred (file added, modified, or deleted), when the provider triggers a re-scan check, then the registry performs a full re-scan

### Error handling preserved (R3)
- [ ] Given a newly added skill has invalid SKILL.md (bad YAML, missing description), when the registry re-scans, then the invalid skill is logged as a warning and skipped, and other valid skills load normally
- [ ] Given the re-scan itself fails (e.g., permission error on the directory), then the registry retains its previous valid state and logs the error

### Transparent to provider (R4)
- [ ] Given the provider calls registry methods (`skills` property, `get_agents_for_skill()`), then the interface is unchanged — the re-scan happens internally before returning results
- [ ] Given the provider is initialized, then no changes to `SkillsContextProvider.__init__()` or `provide()` signature are needed

### Session stability preserved (R6)
- [ ] Given skills are detected at session start, when skills change on disk during the session, then subsequent messages in the same session continue using the originally detected skills — guaranteed by the existing coordinator behavior where `provide()` only runs on the first message of a new session (via `is_new_session` guard), so `refresh()` naturally runs only at session boundaries

### Performance (R7)
- [ ] Given a typical skills directory (< 20 skills), when a re-scan is triggered, then it completes in negligible time relative to the LLM classification call

## Requires

Dependencies:
- None

---

## Detected Impacts

### Affected Features
- **`docs/feature-specs/agent/skills.md`** — Modifies: R1 (Skill registry discovery at startup) expands to include runtime re-scanning; R11 behavior unchanged but now re-detection on new sessions naturally picks up registry changes
- **`docs/feature-designs/agent/skills.md`** — Modifies: SkillRegistry gains reload/re-scan capability; "Skill Body and Path Stored at Registry Init Time" decision evolves to "stored at scan time" (including re-scans); "Provider Owns Its SkillRegistry" decision unchanged but the owned registry is now mutable

### Notes for Reconciliation
- Update R1 in feature spec to mention runtime re-scanning alongside startup discovery
- Update design's Data Flow section to show re-scan trigger path
- Update "Key Decisions" section for body pre-loading to reflect re-scan behavior
