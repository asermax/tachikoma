# Design: DLT-169 - Track External Message IDs Per Session

**Delta Spec**: [../delta-specs/DLT-169.md](../delta-specs/DLT-169.md)
**Status**: Draft

## Purpose

This document explains the design rationale for tracking platform-specific message IDs alongside conversation sessions, enabling reaction-based session switching.

## Problem Context

The system has no mechanism to map external message IDs (e.g., Telegram `message_id`) to internal session IDs. When a user reacts to a message from a previous conversation, there is no way to determine which session that message belonged to, making reaction-based session routing impossible.

## Design Overview

A channel-agnostic external ID mapping table records incoming and outgoing message IDs per session. The Telegram channel captures IDs at ingestion and response finalization. When a reaction arrives, the handler looks up the reacted-to message's external ID, and if it maps to a different session, triggers a session switch before routing.

## Shape

| Part | Mechanism | Flag |
|------|-----------|:----:|
| **S1** | New `session_external_ids` DB table with domain model (`SessionExternalId` frozen dataclass), ORM record (`SessionExternalIdRecord`), and migration SQL | |
| **S2** | Repository methods: `save_external_id(session_id, channel, direction, external_id)` and `lookup_session_by_external_id(channel, external_id) -> str \| None` | |
| **S3** | Registry facade methods wrapping repository: `record_external_id()` and `find_session_by_external_id()` | |
| **S4** | Add `external_id: str \| None = None` field to `TextMessage` and `ReactionMessage` envelopes | |
| **S5** | Telegram incoming: set `external_id` on envelope and call `record_external_id` after session is determined | |
| **S6** | Telegram outgoing: expose finalized message IDs from ResponseRenderer (callback or return value), persist asynchronously after finalization | |
| **S7** | Telegram reaction: extract `event.message_id` from `MessageReactionUpdated`, set on `ReactionMessage` envelope, perform session lookup and switching before routing | |
| **S8** | Coordinator integration: add reaction-based session routing path that closes current session and reopens target session when external ID resolves to a different session | |

## Components

### Implementation Structure

| Layer/Component | Responsibility | Key Decisions |
|-----------------|----------------|---------------|
| DB Model | `session_external_ids` table with cascade delete, unique constraint on `(channel, external_id)` | Raw SQL migration (project convention) |
| Repository | Persist and query external IDs | Insert-or-ignore for duplicates |
| Registry | Facade over repository with graceful degradation | Best-effort, errors logged not raised |
| Message envelope | `external_id` field on `TextMessage` and `ReactionMessage` | Optional field, `None` = no-op |
| Telegram channel (incoming) | Capture `message.message_id`, set on envelope, persist after session determined | Persist after session creation/resume to know session_id |
| Telegram channel (outgoing) | Expose `_current_message_id` + `_split_message_ids` after finalization, persist async | Async to avoid blocking response delivery |
| Telegram channel (reaction) | Extract `event.message_id`, lookup session, switch if needed | Only switches when delivery lock is free (idle) |
| Coordinator | Reaction-based session routing path | Reuses existing `_handle_transition` + `reopen_session` |

### Integration Points

- Telegram channel calls `record_external_id` after session is determined (incoming) and after response finalization (outgoing)
- Telegram reaction handler calls `find_session_by_external_id` before routing; if different session found, triggers coordinator session switch
- Coordinator receives the target session ID from the channel and performs the transition

## Modeling

### SessionExternalId

```
SessionExternalId:
  id: int (auto-increment)
  session_id: str (FK → sessions.id, ON DELETE CASCADE)
  channel: str (canonical: "telegram", "repl")
  direction: str ("incoming" | "outgoing")
  external_id: str

Indexes:
  - UNIQUE (channel, external_id) — insert-or-ignore on duplicate
  - session_id — for session-scoped queries
```

## Data Flow

### Incoming Message

```
Telegram message → _handle_message/media
  → TextMessage(external_id=str(message.message_id))
  → coordinator.enqueue(envelope)
  → coordinator: session creation/resume
  → registry.record_external_id(session_id, "telegram", "incoming", external_id)
```

### Outgoing Message

```
ResponseRenderer finalizes
  → exposes (_current_message_id, _split_message_ids)
  → Telegram channel reads IDs
  → for each ID: registry.record_external_id(session_id, "telegram", "outgoing", str(id))
  → async, errors logged only
```

### Reaction with Session Switch

```
MessageReactionUpdated → _handle_reaction
  → ReactionMessage(external_id=str(event.message_id))
  → registry.find_session_by_external_id("telegram", external_id)
  → if found and != current session:
      → coordinator closes current session (async post-processing)
      → coordinator reopens target session
  → enqueue ReactionMessage
```

## Key Decisions

### Unique constraint with insert-or-ignore

**Choice**: UNIQUE on `(channel, external_id)` with INSERT OR IGNORE
**Why**: The same message ID should never map to two sessions. If a re-processing occurs (e.g., duplicate event), the existing mapping is preserved rather than overwritten.
**Alternatives Considered**:
- No unique constraint: allows stale duplicates, complicates lookups
- UPSERT (replace): could overwrite a valid mapping with a stale one

**Consequences**:
- Pro: Idempotent recording, clean semantics
- Con: First recording wins — if the same external ID somehow reaches two sessions, the first session owns it

### Session switching only when idle

**Choice**: Reaction-based session switching only occurs when the delivery lock is free (agent is idle)
**Why**: Switching sessions mid-response would disrupt the in-flight SDK exchange. The delivery lock already serializes message processing, so mid-stream reactions are steered into the current session as they are today.
**Alternatives Considered**:
- Interrupt mid-response: would require aborting SDK session, complex and risky
- Queue the switch for after response: adds complexity, lock serialization handles this naturally

**Consequences**:
- Pro: Simple, reuses existing locking semantics
- Con: User must wait for current response to complete before session switches via reaction

### Outgoing ID recording is async

**Choice**: Outgoing IDs are recorded asynchronously after the Result event
**Why**: Blocking response delivery on a database write adds unnecessary latency. The mapping is not needed until a future reaction arrives.
**Consequences**:
- Pro: No impact on response delivery latency
- Con: Brief window where outgoing IDs are not yet recorded — if a reaction arrives in this window, the mapping won't exist yet and the reaction routes normally (graceful degradation)

## Open Questions

(None — all mechanisms are understood)

---

## Detected Impacts

### Affected Feature Designs
- **docs/feature-specs/agent/sessions.md** - Adds: external ID table, recording, lookup, session switching via external ID
- **docs/feature-specs/channels/telegram.md** - Modifies: message/media handlers, ResponseRenderer, reaction handler
- **docs/feature-specs/agent/core-architecture.md** - Modifies: envelope types gain `external_id`
- **docs/feature-specs/agent/boundary-detection.md** - Modifies: reaction-based session switching as alternative routing

### Notes for Reconciliation
- sessions.md: add R entries for external ID table and CRUD, add behavior section
- telegram.md: modify incoming/outgoing message behaviors, modify reaction behavior
- core-architecture.md: update envelope type descriptions
- boundary-detection.md: add reaction-based routing note
