# Design: DLT-072 - Fix task management MCP tool bugs

**Delta Spec**: [../delta-specs/DLT-072.md](../delta-specs/DLT-072.md)
**Status**: Draft

## Purpose

This document explains the design rationale for this delta: the modeling choices, data flow, system behavior, and architectural approach.

After implementation, the "Detected Impacts" section will guide reconciliation into feature design docs.

## Problem Context

[What specific problem does this delta solve? What constraints exist?]

## Design Overview

[High-level description of the design approach. What are the main components/concepts?]

## Shape

| Part | Mechanism | Flag |
|------|-----------|:----:|
| **S1** | Add task `id` to `list_tasks` output format (one line per definition, include ID before name) | |
| **S2** | Add `task_type` field to `UpdateTaskArgs` Pydantic model with "session"/"background" validation in `update_task` handler | |
| **S3** | Improve `update_task` error formatting: when catching `TaskRepositoryError`, include both the wrapper message and the underlying `__cause__` in the agent-facing error text | |
| **S4** | Rewrite `@tool()` description strings for all four tools — include parameter types, required/optional, valid values, and examples | |
| **S5** | Rewrite preamble Tasks section in `SYSTEM_PREAMBLE` — correct `notify` behavior, add cross-references between tools, clarify when to use each | |

### Flagged Unknowns

None.

## Components

### Implementation Structure

| Layer/Component | Responsibility | Key Decisions |
|-----------------|----------------|---------------|

### Cross-Layer Contracts

**Integration Points**:

### Shared Logic

## Modeling

[How is the domain modeled? What are the key entities/concepts? What relationships exist?]

## Data Flow

[How does data move through the system? What are the key paths?]

## Key Decisions

## System Behavior

## Open Questions

- [ ] None identified

---

## Detected Impacts

### Affected Feature Designs
- **docs/feature-designs/tasks/task-management.md** - Modifies: task listing flow to include IDs; MCP tools component description to reflect enriched descriptions and `task_type` on update; preamble key decision section to reflect corrected `notify` docs

### Notes for Reconciliation
- Task listing flow needs updating to include IDs in output format
- Tools component table needs `task_type` on update and enriched descriptions
- Preamble key decision section needs corrected `notify` documentation

## Notes

[Any additional context, links to research, related decisions]
