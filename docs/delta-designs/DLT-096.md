# Design: DLT-096 - Include Last Exchange in Session Resumption Candidates

**Delta Spec**: [../delta-specs/DLT-096.md](../delta-specs/DLT-096.md)
**Status**: Draft

## Purpose

This document explains the design rationale for this delta: the modeling choices, data flow, system behavior, and architectural approach.

After implementation, the "Detected Impacts" section will guide reconciliation into feature design docs.

## Problem Context

[What specific problem does this delta solve? What constraints exist?]

## Design Overview

[High-level description of the design approach. What are the main components/concepts?]

## Shape

| Part | Mechanism | Flag |
|------|-----------|:----:|
| **S1** | Add `last_exchange` nullable text field to Session/SessionRecord model + Alembic migration | |
| **S2** | Create `LastExchangeProcessor` (`MessagePostProcessor`) that persists `agent_response` to `session.last_exchange` via registry | |
| **S3** | Add `last_exchange: str | None` to `SessionCandidate` dataclass | |
| **S4** | Update `_query_resume_candidates()` in coordinator to pass `last_exchange` from session records into `SessionCandidate` | |
| **S5** | Pass last exchange data to boundary detection (coordinator passes `active.last_exchange` to `detect_boundary()`) | |
| **S6** | Update boundary detection prompt templates to include last exchange for current session and candidates | |

## Components

### Implementation Structure

| Layer/Component | Responsibility | Key Decisions |
|-----------------|----------------|---------------|
| | | |

### Cross-Layer Contracts

**Integration Points**:
-

### Shared Logic

-

## Modeling

[How is the domain modeled? What are the key entities/concepts? What relationships exist?]

## Data Flow

[How does data move through the system? What are the key paths?]

## Key Decisions

### [Decision Name]

**Choice**: [What was chosen]
**Why**: [Rationale for this choice]
**Alternatives Considered**:
- [Alternative 1]: [Why not chosen]

**Consequences**:
- Pro: [benefit]

## System Behavior

[How does the system behave in important scenarios? Edge cases?]

## Open Questions

- [ ]

---

## Detected Impacts

### Affected Feature Designs
- **docs/feature-specs/agent/boundary-detection.md** - Modifies: adds `last_exchange` to boundary detection input
- **docs/feature-specs/agent/sessions.md** - Modifies: adds `last_exchange` field to session model
- **docs/feature-specs/agent/post-processing-pipeline.md** - Modifies: references new per-message processor

### Notes for Reconciliation
- boundary-detection.md: Add requirement about `last_exchange`; update Session Resumption Matching section
- sessions.md: Add `last_exchange` to session model fields; add behavior for `last_exchange` update
- post-processing-pipeline.md: Mention `LastExchangeProcessor` as registered per-message processor

## Notes

[Any additional context, links to research, related decisions]
