# Design: DLT-029 - Complete pending signals lifecycle with removal and auto-injection

**Delta Spec**: [../delta-specs/DLT-029.md](../delta-specs/DLT-029.md)
**Status**: Draft

## Purpose

This document explains the design rationale for this delta: the modeling choices, data flow, system behavior, and architectural approach.

After implementation, the "Detected Impacts" section will guide reconciliation into feature design docs.

## Problem Context

## Design Overview

## Shape

| Part | Mechanism | Flag |
|------|-----------|:----:|
| **S1** | Read pending signals pre-fork and embed them (numbered S1..Sn) into a dynamic section of `CONTEXT_UPDATE_PROMPT` | |
| **S2** | Remove `read_pending_signals` tool definition from `tools.py` and `create_pending_signals_server()` | |
| **S3** | Add `remove_pending_signal` tool accepting one or more indices; uses pre-fork signal snapshot to map indices to text; removes matching entries from file | |
| **S4** | Update `CONTEXT_UPDATE_PROMPT` instructions to describe the full lifecycle (stage → promote+remove → cleanup stale) | |
| **S5** | Update `add_pending_signal` and `remove_pending_signal` tool descriptions to convey lifecycle roles | |

### Flagged Unknowns

None — all mechanisms are well-understood, scoped to two existing files with clear patterns to follow.

## Components

## Modeling

## Data Flow

## Key Decisions

## System Behavior

## Open Questions

---

## Detected Impacts

### Affected Feature Designs

### Notes for Reconciliation

## Notes
