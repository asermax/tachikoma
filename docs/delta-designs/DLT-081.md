# Design: DLT-081 - Workflow State Machine for Skills

**Delta Spec**: [../delta-specs/DLT-081.md](../delta-specs/DLT-081.md)
**Relevant ADRs**: ADR-007 (persistence), ADR-008 (system prompt composition)
**Relevant DES**: DES-003 (bootstrap hooks), DES-004 (prompt-driven processors), DES-006 (MCP tool server factory)
**Status**: Draft

## Purpose

This document explains the design rationale for the workflow state machine: how workflows are defined within skills, how state is tracked, how the MCP tools enforce the state machine boundary, and how the system integrates with existing Tachikoma infrastructure.

## Problem Context

Skills that define multi-step workflows (morning routines, deployment processes, reading list processing) rely entirely on the LLM to remember which steps are done and what comes next. Without explicit state, the agent skips steps, repeats completed ones, or loses its place after context compaction. The solution uses a file-tree-based approach where workflows are directory structures the agent navigates natively, with MCP tools enforcing state transitions and a database table persisting state across context boundaries.

## Design Overview

Workflows are optional subdirectories within skills (`workflows/<name>/`), each containing ordered step directories with instructions, references, and scripts. Five MCP tools (`start_workflow`, `update_workflow_state`, `get_workflow_state`, `end_workflow`, `list_active_workflows`) form the state machine boundary — the agent cannot change workflow state except through these tools, which validate all transitions. State is persisted in a database table, making it resilient to context compaction and process restarts. Stale workflow cleanup runs as a post-processor in the `pre_finalize` phase, committed alongside session changes.

## Shape

| Part | Mechanism | Flag |
|------|-----------|:----:|
| **S1** | Workflow definition loader — discovers and parses `workflows/` directories within skills, reads step subdirectories with their instructions.md frontmatter, references/, and scripts/ | |
| **S2** | Workflow state database model — SQLAlchemy model for workflow instances (id, skill_name, workflow_name, current_step, step_states JSON, definition_snapshot JSON, created_at, updated_at) | |
| **S3** | `start_workflow` MCP tool — creates DB record with definition snapshot, returns step list + agent guidance (including instruction to persist workflow ID durably in scratchpad) | |
| **S4** | `update_workflow_state` MCP tool — validates transitions against step properties (start/complete/skip actions), advances state, returns `step_path` + instructions content only | |
| **S5** | `get_workflow_state` MCP tool — read-only lookup returning full workflow state for resumption | |
| **S6** | `end_workflow` MCP tool — marks workflow completed/aborted, removes DB record | |
| **S7** | Stale workflow cleanup — post-processor in `pre_finalize` phase, removes workflows with `updated_at` older than configurable threshold (default 24h), cleanup committed with session changes | |
| **S8** | System preamble workflow section — static text explaining workflows and their MCP tools (no active state injection) | |
| **S9** | Built-in `workflow-authoring-guide` skill — directory with SKILL.md and references for creating workflow definitions | |
| **S10** | Skill registry extension — expose workflow definitions alongside skill data so MCP tools can look them up by name | |
| **S11** | `list_active_workflows` MCP tool — returns all active workflows with id, skill_name, workflow_name, current_step for recovery after context loss | |

## Components

### Implementation Structure

| Layer/Component | Responsibility | Key Decisions |
|-----------------|----------------|---------------|
| Workflow Definition Loader | Parse skill `workflows/` directories, load step metadata from frontmatter | Uses python-frontmatter (consistent with skill loading) |
| Workflow State Model | SQLAlchemy model for active workflow instances | Follows ADR-007 (SQLAlchemy async + aiosqlite), JSON column for step states |
| MCP Tool Server | Expose workflow lifecycle tools to the agent | Follows DES-006 (MCP tool server factory pattern) |
| Skill Registry Extension | Expose workflow definitions for lookup by MCP tools | Extends existing SkillRegistry, no new registry needed |
| System Preamble | Static workflow awareness section in system prompt | Follows ADR-008 (system prompt composition via append) |
| Built-in Skill | Workflow authoring guide | Follows existing built-in skill pattern (skill-authoring-guide) |
| Stale Cleanup Processor | Remove old workflow state records | Post-processor in `pre_finalize` phase (DES-004), runs before git commit |

### Cross-Layer Contracts

**MCP Tool Schemas**:

```
start_workflow(skill_name: str, workflow_name: str)
  → { workflow_id: str, steps: [{id, title, skippable}], guidance: str }
  | { error: str, existing_workflow_id: str }  (duplicate prevention)

update_workflow_state(workflow_id: str, step: str, action: "start" | "complete" | "skip")
  → { step: str, status: str, step_path: str, instructions: str }
  | { workflow_complete: true, message: str }
  | { error: str }

get_workflow_state(workflow_id: str)
  → { workflow_id: str, skill_name: str, workflow_name: str, current_step: str, steps: [{id, title, status}], created_at: str, updated_at: str }
  | { error: str }

end_workflow(workflow_id: str, action: "complete" | "abort")
  → { message: str }
  | { error: str }

list_active_workflows()
  → { workflows: [{workflow_id: str, skill_name: str, workflow_name: str, current_step: str}] }
  | { workflows: [] }
```

**Transition validation rules** (enforced by `update_workflow_state`):
- `start`: step must be `pending`; marks as `started`; returns step_path + instructions
- `complete`: step must be `started` (not `pending` — must start before completing); marks as `completed`; advances to next pending step
- `skip`: step must be `skippable: true` in frontmatter AND `pending`; marks as `skipped`; advances to next pending step
- Any action on a completed/skipped step: error with explanation
- Action on a non-existent step or invalid workflow ID: error listing valid step IDs
- All actions update `updated_at` on the workflow state record

**Integration Points**:
- MCP tools call skill registry to resolve skill_name → workflow definition
- MCP tools call workflow state repository for all DB operations
- Stale cleanup runs as post-processor in `pre_finalize` phase, before GitProcessor in `finalize`
- System preamble static text added to preamble template
- `start_workflow` guidance instructs agent to persist workflow ID in scratchpad file
- Workflow MCP tools registered alongside task MCP tools in coordinator's `mcp_servers` dict
- DB errors surface as MCP tool errors via `{"is_error": true, ...}` (consistent with task tools pattern)

### Shared Logic

- **Workflow definition model**: Parsed step structure (id, title, instructions path, references, scripts, frontmatter properties) shared between loader and MCP tools
- **Step state enum**: `pending | started | completed | skipped` — shared between state model and transition validation
- **Transition validation**: Central logic in MCP tool handler, enforcing step ordering and frontmatter properties

## Modeling

```
WorkflowDefinition (filesystem):
  skill_name: str
  workflow_name: str
  steps: [StepDefinition]

StepDefinition (filesystem):
  id: str (directory name, e.g., "01-plan")
  title: str (from frontmatter)
  instructions_path: Path
  references_path: Path | None
  scripts_path: Path | None
  skippable: bool (default false)
  properties: dict (extensible frontmatter fields)

WorkflowState (database — table: `workflow_states`):
  id: str (UUID, PK)
  skill_name: str (Indexed — for duplicate prevention lookup)
  workflow_name: str (Indexed — for duplicate prevention lookup)
  current_step: str | None
  step_states: JSON ({"01-plan": "completed", "02-execute": "started", ...})
  definition_snapshot: JSON (serialized step definitions at start time)
  created_at: datetime
  updated_at: datetime

  Unique constraint: (skill_name, workflow_name) — enforces one active instance per workflow definition
```

## Data Flow

```
1. Skill detection loads skill with workflows/
2. Agent reads SKILL.md → discovers available workflows
3. Agent calls start_workflow(skill_name, workflow_name)
   → Loader parses workflow definition from skill directory
   → Creates WorkflowState record in DB (with definition snapshot)
   → Returns step list + agent guidance (TodoWrite, scratchpad with workflow ID)
4. Agent persists workflow ID in scratchpad file
5. Agent creates TodoWrite tasks for steps
6. Agent calls update_workflow_state(id, step, "start")
   → Validates step exists and is next in order
   → Updates step_states in DB
   → Returns step_path + instructions content (instructions.md body)
7. Agent executes step, using step_path to navigate references/ and scripts/ as needed
8. Agent calls update_workflow_state(id, step, "complete")
   → Marks step completed, advances to next step
   → Returns next step_path + instructions (or completion message)
9. Repeat 6-8 until all steps done
10. Agent calls end_workflow(id, "complete") → DB record removed
```

**Recovery flow (context loss)**:
```
1. Agent loses workflow ID (context compaction, session corruption)
2. Agent reads scratchpad file → recovers workflow ID
   OR (scratchpad also lost):
3. Agent calls list_active_workflows() → recovers workflow ID
4. Agent calls get_workflow_state(id) → full state for resumption
```

## Key Decisions

### File tree as workflow definition

**Choice**: Workflows mapped to directory structures the agent navigates natively
**Why**: Resilient to model capability changes — if a model can handle multiple steps in one go, steps can be condensed without changing the system
**Alternatives Considered**:
- YAML manifest with step definitions: More structured but creates a parallel format the agent must learn
- Database-only definitions: Loses the filesystem alignment that makes skills intuitive

**Consequences**:
- Pro: Agent already knows how to navigate directories; aligns with skill folder pattern
- Pro: Steps are self-contained with their own references and scripts
- Con: Ordering relies on naming convention (alphabetical sort with numeric prefix)

### MCP tools as state machine boundary

**Choice**: All state transitions go through MCP tools that validate and enforce rules
**Why**: Prevents the agent from corrupting state; tools return step-specific instructions at transition points, guiding the agent naturally
**Alternatives Considered**:
- Context-injected state with agent self-management: Relies on LLM discipline, defeats the purpose
- Event-driven state machine: Over-engineered for the use case

**Consequences**:
- Pro: Strong state guarantees regardless of agent capability
- Pro: Tool results naturally guide the agent to the next action
- Con: Agent must call tools correctly (but tool errors provide clear guidance)

### Step path + instructions only in tool response

**Choice**: `update_workflow_state` returns the step's directory path and instructions.md content — no explicit references/scripts listing
**Why**: The step's instructions.md is responsible for guiding the agent to its own references and scripts. Returning only the step path and instructions keeps tool responses minimal and lets the step instructions be the single source of guidance. The agent navigates from step_path to access any supporting files as needed.
**Alternatives Considered**:
- Full content inline (instructions + references list + scripts list): Bloats tool responses; assumes the agent always needs all resources
- Paths only (no content): Requires an extra file read for instructions the agent always needs
- Hybrid (instructions inline, references/scripts as paths): More explicit but redundant when instructions already reference their own resources

**Consequences**:
- Pro: Minimal tool response size — only what the agent needs to proceed
- Pro: Step instructions are the single source of guidance — no duplication between instructions and tool response
- Pro: Agent navigates file tree naturally using the step_path
- Con: Agent must read instructions to discover reference/script paths (natural behavior)

### Definition snapshot at start time

**Choice**: Workflow definition is serialized when started and stored with the state record
**Why**: If a skill's workflow definition changes while a workflow is active, the active workflow uses the original definition — preventing mid-flight inconsistencies
**Alternatives Considered**:
- Always read from filesystem: Could cause steps to appear/disappear mid-workflow
- Version workflow definitions: Adds complexity without clear benefit for current scope

**Consequences**:
- Pro: Active workflows are insulated from definition changes
- Con: Definition snapshot increases DB record size (mitigated by small step definitions)

### Database table for state (not filesystem)

**Choice**: Workflow state persisted in a SQLAlchemy database table
**Why**: Consistent with existing persistence pattern (ADR-007), supports concurrent workflows, queryable, and benefits from existing database infrastructure
**Alternatives Considered**:
- JSON files in `.tachikoma/state/workflows/`: Simpler but loses queryability and transactional safety

**Consequences**:
- Pro: Follows established patterns, no new persistence mechanism
- Pro: Atomic updates, concurrent access safety
- Con: Adds a migration for the new table (follows existing Alembic pattern)

### Workflows outlive sessions (no session_id on WorkflowState)

**Choice**: Workflow state records have no `session_id` field — workflows are independent of sessions
**Why**: Workflows may span multiple messages exchanges within a session, The agent starts a workflow, interacts with it across many turns, and the session. If the session ends and a new one begins via boundary detection, the workflow should persist and be resumable via `get_workflow_state`. Coupling workflows to sessions would make cleanup harder ( as session-scoped cleanup could destroy active workflows prematurely.
**Alternatives Considered**:
- Add `session_id` to WorkflowState: Couples workflows to session lifecycle; session close would need to decide what to do with active workflows

**Consequences**:
- Pro: Workflows survive session transitions naturally
- Pro: No coordination needed between session management and workflow management
- Con: `list_active_workflows` may return workflows from previous sessions (agent must correlate by context)

### Post-processor for stale cleanup

**Choice**: Stale workflow cleanup runs as a post-processor in the `pre_finalize` phase, before GitProcessor commits
**Why**: Session close already triggers post-processing, so cleanup piggybacks on existing lifecycle without additional infrastructure (no background tasks, no startup hooks). Running in `pre_finalize` ensures cleanup happens before the git commit, so removed workflow records are included in the session's commit.
**Alternatives Considered**:
- Startup-only bootstrap hook: Only runs on process restart; long-running sessions accumulate stale records
- Periodic background task: Adds infrastructure complexity for a low-frequency concern
- Lazy cleanup on tool access: Mixes cleanup with read operations, unpredictable

**Consequences**:
- Pro: No additional infrastructure — reuses existing post-processing pipeline
- Pro: Cleanup committed atomically with session changes
- Con: Cleanup frequency tied to session close rate (acceptable for 24h threshold)

### list_active_workflows as recovery safety net

**Choice**: A dedicated `list_active_workflows` tool returns all active workflow IDs and metadata
**Why**: The primary recovery mechanism is the agent persisting the workflow ID in its scratchpad. But if the scratchpad is also lost (session corruption, topic switch with context loss), the agent needs a way to discover active workflows without knowing any IDs.
**Alternatives Considered**:
- Preamble injection of active workflow IDs: Pollutes the system prompt with dynamic state; breaks static preamble pattern
- get_workflow_state with wildcard: Overloaded semantics; tool is designed for known IDs
- No recovery mechanism: Accepts that lost IDs mean abandoned workflows (poor UX)

**Consequences**:
- Pro: Simple safety net — one tool call recovers all active workflow IDs
- Pro: Keeps system preamble static (ADR-008)
- Con: Agent must know to call it (mitigated by start_workflow guidance mentioning recovery)

### start_workflow guidance as tunable prompt engineering

**Choice**: The `start_workflow` tool constructs a guidance string that instructs the agent to use TodoWrite, persist the workflow ID in a scratchpad file, and call `update_workflow_state` to progress
**Why**: This guidance is the primary mechanism for R8 (agent task tracking) and R9 (scratchpad). The quality of this guidance directly affects workflow reliability. However, the guidance is prompting, not enforcement — it must be treated as a tunable parameter that can be refined based on agent behavior.
**Alternatives Considered**:
- Hardcoded guidance template: Simpler but less adaptable
- Dedicated MCP tool for scratchpad: Over-engineered for current scope

**Consequences**:
- Pro: Guidance adapts as agent behavior is observed in practice
- Pro: No additional infrastructure needed
- Con: Guidance quality is directly affects adoption (mitigated by iteration)

## System Behavior

### Scenario: Workflow resumption after context compaction

**Given**: An active workflow with ID "abc-123" at step "03-review"
**When**: Context compaction occurs and the agent loses the workflow context
**Then**: The agent reads its scratchpad file to recover the workflow ID, calls `get_workflow_state("abc-123")`, receives the full state, and resumes from step "03-review"
**Rationale**: State lives in the database, not conversation memory. The scratchpad file survives context compaction because it's on disk.

### Scenario: Full context loss recovery

**Given**: An active workflow where both the conversation context and scratchpad are lost
**When**: The agent needs to resume but has no workflow ID
**Then**: The agent calls `list_active_workflows()`, identifies the relevant workflow, and calls `get_workflow_state(id)` to resume
**Rationale**: `list_active_workflows` is the safety net for total context loss scenarios.

### Scenario: Attempting to skip a non-skippable step

**Given**: An active workflow where step "02-execute" has `skippable: false` in its frontmatter
**When**: The agent calls `update_workflow_state(id, "02-execute", "skip")`
**Then**: The tool returns an error explaining the step cannot be skipped
**Rationale**: Frontmatter properties enforce workflow integrity at the tool boundary

### Scenario: Stale workflow cleanup

**Given**: A workflow state record with `updated_at` 25 hours ago (default threshold: 24h)
**When**: The post-processor runs during session close (pre_finalize phase)
**Then**: The record is removed from the database before the git commit
**Rationale**: Piggybacks on existing post-processing lifecycle; cleanup is committed with session changes

### Scenario: Definition changes during active workflow

**Given**: An active workflow started from a 3-step definition
**When**: The skill author modifies the workflow to have 4 steps
**Then**: The active workflow continues with the original 3-step definition (from snapshot)
**Rationale**: Mid-flight definition changes would corrupt in-progress state

### Scenario: Agent navigates step resources

**Given**: An active workflow at step "02-execute" with references/ and scripts/ subdirectories
**When**: `update_workflow_state` returns `step_path` and instructions content
**Then**: The instructions guide the agent to use `step_path` as the base for accessing references and scripts. The agent read reference files and executes scripts using paths relative to `step_path`.
**Rationale**: Step instructions are the single source of guidance — they reference their own resources rather than the tool listing them separately.

### Scenario: Stale workflow during long-running session

**Given**: An active workflow at step "03-review" with `updated_at` 23 hours ago, and a session that remains open for 24h+
**When**: The post-processor runs during session close
**Then**: The record is removed from the database
**Rationale**: 24h without updates signals the workflow is abandoned. The session being open doesn't prevent cleanup — the stale threshold measures idleness, not session activity.

### Scenario: Duplicate workflow prevention

**Given**: An active workflow for skill "morning-routine" workflow "morning-routine"
**When**: `start_workflow("morning-routine", "morning-routine")` is called again
**Then**: The tool returns an error with the existing workflow ID, indicating the workflow is already running
**Rationale**: One active instance per skill+workflow name prevents confusion from duplicate workflows running concurrently

---

## Detected Impacts

### Affected Feature Designs
- **docs/feature-designs/agent/skills.md** - Adds: workflow directory structure, workflow definition model, registry extension for workflow lookup
- **docs/feature-designs/agent/core-architecture.md** - Adds: workflow MCP tools registration, preamble workflow section
- **docs/feature-designs/agent/skills.md** - Adds: workflow directory structure, workflow definition model, registry extension for workflow lookup
- **docs/feature-designs/agent/core-architecture.md** - Adds: workflow MCP tools registration, preamble workflow section
- **docs/feature-designs/agent/post-processing-pipeline.md** - Adds: stale workflow cleanup processor in pre_finalize phase
- **docs/feature-designs/tasks/task-management.md** - Modifies: clarification between workflow MCP tools and task MCP tools (distinct MCP servers registrations, not replacement)

### Notes for Reconciliation
- `skills.md` design needs new section for workflow structure, step definition model, and registry extension
- `core-architecture.md` design needs MCP tool factory registration for 5 workflow tools
- `pre-processing-pipeline.md` design may need ContextResult extension for active workflows
- `post-processing-pipeline.md` design needs new processor registration in pre_finalize phase
- `task-management.md` design needs note clarifying workflow tools vs task tools distinction

## Notes

- Inspired by the "file tree as agent workflow" concept (https://youtube.com/shorts/tbVtt2-qUJo) — mapping workflows to directory structures that coding agents navigate natively
- Workflow steps use the SDK's native TodoWrite for progress tracking, which is separate from Tachikoma's task management system
- The scratchpad concept is prompting-based — the agent is instructed to maintain notes in a file, not through a dedicated MCP tool
- `list_active_workflows` is intentionally simple (no filtering, no pagination) — it's a recovery tool, not a management tool
- The guidance text returned by `start_workflow` is a tunable parameter — its quality directly affects the reliability of the workflow system. The guidance content should be treated as a prompt engineering concern and refined based on observed agent behavior during use
- Scratchpad file location convention: `.tachikoma/scratchpads/<workflow_id>.md` — agents persist workflow IDs and a file relative to workspace root. The `.tachikoma/scratchpads/` directory is where agents recover workflow IDs across session boundaries
