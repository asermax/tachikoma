# Design: DLT-041 - Persist Session Context to Database

**Delta Spec**: [../delta-specs/DLT-041.md](../delta-specs/DLT-041.md)
**Status**: Draft

## Purpose

This document explains the design rationale for this delta: the modeling choices, data flow, system behavior, and architectural approach.

After implementation, the "Detected Impacts" section will guide reconciliation into feature design docs.

## Problem Context

## Design Overview

## Shape

| Part | Mechanism | Flag |
|------|-----------|:----:|
| **S1** | `SessionContextEntry` domain dataclass + `SessionContextRecord` ORM model (mirrors Session's dual-model pattern) | |
| **S2** | `SessionContextRepository` with save-batch and load-by-session methods | |
| **S3** | Coordinator integration: save foundational entries on session creation, save pre-processing entries after pipeline completes | |
| **S4** | Structured foundational context: extend `load_context()` / `context_hook()` to return individual file entries alongside the assembled prompt string, so the coordinator receives both | |
| **S5** | Database migration: new `session_context` table with FK to sessions | |

### Flagged Unknowns

None — all unknowns resolved during spec phase.

## Components

### Implementation Structure

### Cross-Layer Contracts

### Shared Logic

## Modeling

## Data Flow

## Key Decisions

## System Behavior

## Open Questions

- [ ]

---

## Detected Impacts

### Affected Feature Designs

### Notes for Reconciliation

## Notes
