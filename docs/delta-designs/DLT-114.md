# Design: DLT-114 - Set TZ environment variable for agent subprocesses

**Delta Spec**: [../delta-specs/DLT-114.md](../delta-specs/DLT-114.md)
**Status**: Draft

## Purpose

This document explains the design rationale for this delta: the modeling choices, data flow, system behavior, and architectural approach.

After implementation, the "Detected Impacts" section will guide reconciliation into feature design docs.

## Problem Context

## Design Overview

## Shape

| Part | Mechanism | Flag |
|------|-----------|:----:|
| **S1** | Extend `merge_env()` to accept an optional `timezone` parameter; when provided, inject `TZ={timezone}` into the merged env **before** user env, so user-provided `TZ` in `[agent.env]` overrides it | |
| **S2** | Update `__main__.py` call site to pass `settings.tasks.timezone` to `merge_env()` | |
| **S3** | Simplify system prompt date command in `context/loading.py` — remove `TZ='{timezone}'` prefix, keep just `date '+%A, %B %d, %Y at %H:%M:%S %Z (%z)'` | |
| **S4** | Add/update tests: `merge_env` injects TZ, user override wins, system prompt no longer includes TZ prefix | |

## Components

### Implementation Structure

### Cross-Layer Contracts

### Shared Logic

## Modeling

## Data Flow

## Key Decisions

## System Behavior

## Open Questions

---

## Detected Impacts

### Affected Feature Designs

### Notes for Reconciliation

## Notes
