# Implementation Plan: DLT-086

**Delta Spec**: [../delta-specs/DLT-086.md](../delta-specs/DLT-086.md)
**Delta Design**: [../delta-designs/DLT-086.md](../delta-designs/DLT-086.md)
**Status**: Not Started

## Batch 1: Session registry — `can_reopen_session` validation method — ✓ Done
Depends on: — (none)

### Context & Research
- `docs/delta-specs/DLT-086.md` R7 — requirement for handling sessions that cannot be reopened, which drives the need for pre-validation
- `docs/delta-designs/DLT-086.md` S4 — design for `can_reopen_session` as a validation-only method checking the same preconditions as `reopen_session`
- `src/tachikoma/sessions/registry.py:194-254` — `reopen_session` implementation containing the exact validation preconditions to mirror: session exists, transcript_path not None, file exists on disk, within max_session_age, is closed (ended_at not None), not already active
- `tests/sessions/test_registry.py:416` — existing `TestSessionRegistryReopenSession` test class showing the test patterns and mock setup for reopen validation

### Out of Scope
- No schema changes, no coordinator changes, no channel changes
- Does not refactor `reopen_session` to call `can_reopen_session` (can be done later if desired)

### Steps

#### Step 1: Add `can_reopen_session` method to SessionRegistry

- Create: `src/tachikoma/sessions/registry.py` (after `reopen_session`, around line 276)
- What to do: Add `async def can_reopen_session(self, session_id: str) -> bool` that performs the same read-only validation checks as the first half of `reopen_session` (lines 206-254) without any side effects:
  - Fetch session by ID, return False if not found
  - Check transcript_path is not None, return False if None
  - Check transcript file exists on local filesystem, return False if missing
  - Check session.started_at is within max_session_age, return False if too old
  - Check session.ended_at is not None (session is closed), return False if still open
  - Check session is not already the active session, return False if active
  - If all checks pass, return True
  - Wrap in try/except like `find_session_by_external_id` — log warnings, return False on errors
- Test: `just test tests/sessions/test_registry.py -k "can_reopen"`

#### Step 2: Add tests for `can_reopen_session`

- Create: `tests/sessions/test_registry.py` (new class `TestSessionRegistryCanReopenSession` alongside existing `TestSessionRegistryReopenSession`)
- What to do: Add tests mirroring the existing reopen validation tests, but asserting True/False instead of Session/None:
  - `test_returns_false_when_session_not_found` — session ID doesn't exist in DB
  - `test_returns_false_when_no_transcript_path` — session has transcript_path=None
  - `test_returns_false_when_transcript_file_missing` — transcript_path set but file doesn't exist
  - `test_returns_false_when_session_too_old` — started_at exceeds max_session_age
  - `test_returns_false_when_session_still_open` — ended_at is None
  - `test_returns_false_when_session_is_active` — session is the current active session
  - `test_returns_true_for_valid_closed_session` — all checks pass, transcript exists and is within age
  - Use the same mock patterns from `TestSessionRegistryReopenSession` (db fixture with Session objects, tmp_path for transcript files, timedelta for age)
- Test: `just test tests/sessions/test_registry.py -k "can_reopen"`

---

## Batch 2: Coordinator — pre-validation before session close — ✓ Done
Depends on: Batch 1

### Context & Research
- `docs/delta-specs/DLT-086.md` R7, AC (Failed session resume) — acceptance criteria for validation failure behavior: error message to user, no session close, fall through to normal routing
- `docs/delta-designs/DLT-086.md` S4, Key Decision "Coordinator-level resume validation" — design rationale for centralizing validation in `_route_to_target_session`, returning False on failure, yielding Status event
- `docs/delta-designs/DLT-086.md` "Status event for resume failure" — decision to use existing `Status` AgentEvent for error messaging
- `src/tachikoma/coordinator.py:950-973` — current `_route_to_target_session` implementation that needs the pre-validation check before `_close_and_fire_postprocessing`
- `src/tachikoma/coordinator.py:492-496` — `send_message` caller that unpacks the routing result and needs to handle validation failure
- `src/tachikoma/events.py:38-47` — `Status` dataclass definition (`message: str` field); used for yielding the error message on validation failure
- `tests/test_coordinator.py:2205-2236` — existing `test_target_invalid_session_falls_back_to_fresh` that tests fallback to fresh session; needs rework for new behavior (Status + preserve current instead of fresh session)
- `tests/test_coordinator.py:2270-2296` — existing `test_target_no_active_session_reopen_fails` that also tests fallback behavior; needs similar update
- `tests/test_coordinator.py:386-402` — `_make_mock_registry` helper that needs `can_reopen_session = AsyncMock(return_value=True)` added so existing tests pass unchanged
- DES-014 (generic routing fields) — the `target_session_id` field on all envelope subtypes that triggers this routing path
- Note: `_route_to_target_session` has only one caller (`send_message` at line 494), so the return type change from `tuple[Session | None, bool]` to `tuple[Session | None, bool, bool]` is fully contained. Telegram-level reaction tests (`tests/telegram/test_reactions.py`) test envelope construction at the channel level and do NOT need changes — the coordinator-level behavior change is covered by Batch 2 coordinator tests

### Out of Scope
- Channel-level changes (those go in Batch 3)
- Registry changes (completed in Batch 1)

### Steps

#### Step 3: Modify `_route_to_target_session` to validate before close

- Modify: `src/tachikoma/coordinator.py` (line 950)
- What to do: Change `_route_to_target_session` to:
  - Change return type from `tuple[Session | None, bool]` to `tuple[Session | None, bool, bool]` (active, is_new_session, routed_successfully)
  - After the same-session no-op check (line 960), add `can_reopen_session` call: `valid = await self._registry.can_reopen_session(target_id)`
  - If `valid` is False: log a warning, return `(active, is_new_session, False)` — no state changes, no session close
  - If `valid` is True: proceed with existing logic (close current, reopen target), return `(reopened, False, True)` on success
  - On the existing fallback path (reopen returns None after validation passed — race condition): clear state, create fresh session, return `(fresh, True, True)` — this is a different failure mode from validation failure
  - Update docstring to reflect the new behavior and return type

  ```python
  # After same-session no-op check, before closing current:
  # Pre-validate target before closing current session
  if not await self._registry.can_reopen_session(target_id):
      _log.warning("target_session_id validation failed, preserving current session")
      return active, is_new_session, False
  ```
- Test: `just test tests/test_coordinator.py -k "target_session"`

#### Step 4: Update `send_message` caller to handle validation failure

- Modify: `src/tachikoma/coordinator.py` (line 492)
- What to do:
  - Unpack the third return value: `active, is_new_session, routed = await self._route_to_target_session(...)`
  - If `not routed`: yield `Status(message="Could not resume that conversation — its context is no longer available.")`
  - The message then continues through the rest of `send_message` with the preserved active session — boundary detection, pre-processing, and agent call all proceed normally

  ```python
  if msg.target_session_id is not None and self._registry is not None:
      active, is_new_session, routed = await self._route_to_target_session(
          msg.target_session_id, active, is_new_session
      )
      if not routed:
          yield Status(message="Could not resume that conversation — its context is no longer available.")
  ```
- Test: `just test tests/test_coordinator.py -k "target_session"`

#### Step 5: Update `_make_mock_registry` helper and existing tests

- Modify: `tests/test_coordinator.py:386-402`
- What to do:
  - Add `can_reopen_session = AsyncMock(return_value=True)` to `_make_mock_registry` — default to valid so existing tests pass unchanged
  - Rename and rework `test_target_invalid_session_falls_back_to_fresh` → `test_target_stale_session_yields_status_preserves_current`:
    - Set `registry.can_reopen_session.return_value = False`
    - Assert: current session is NOT closed (`close_session` not called), NO fresh session created, Status event is yielded in events
  - Update `test_target_no_active_session_reopen_fails` (line 2270):
    - This test has no active session, so `can_reopen_session` returning False should still prevent the reopen attempt and yield Status
  - Verify all other target_session_id tests still pass with `can_reopen_session` defaulting to True
- Test: `just test tests/test_coordinator.py -k "target_session"`

#### Step 6: Add new coordinator tests for validation failure behavior

- Create: `tests/test_coordinator.py` (add to existing `TestTargetSessionRouting` class)
- What to do: Add tests covering the new failure modes:
  - `test_target_stale_session_yields_status_preserves_current` — `can_reopen_session` returns False, current session stays active, Status event yielded, message processes normally in current session
  - `test_target_stale_session_no_close` — verify `close_session` is NOT called when `can_reopen_session` returns False
  - `test_reaction_to_stale_session_yields_status` — ReactionMessage with target_session_id pointing to stale session yields Status (behavior change from old silent fallback)
- Test: `just test tests/test_coordinator.py -k "target_session"`

---

## Batch 3: Telegram channel — reply-to extraction and routing — ✓ Done
Depends on: Batch 2

### Context & Research
- `docs/delta-specs/DLT-086.md` R1-R9 — all requirements for reply detection, session lookup, routing, busy-channel behavior, and command precedence
- `docs/delta-specs/DLT-086.md` AC (Reply detection, Session routing, Busy-channel, Interaction with existing features) — acceptance criteria to verify
- `docs/delta-specs/DLT-086.md` User Flow / Breadboard — visual flow diagram showing all decision branches
- `docs/delta-designs/DLT-086.md` S1-S5 — shape parts for extraction helper, session lookup, busy-channel deferral, and command precedence
- `docs/delta-designs/DLT-086.md` Data Flow (Reply-to routing idle, busy, with command) — three detailed flow diagrams
- `docs/delta-designs/DLT-086.md` Scenario sections — all scenario descriptions with rationale
- `src/tachikoma/telegram/__init__.py:1028-1086` — `_handle_reaction` method: the exact pattern to mirror for session lookup (lines 1058-1073) and idle-path deferred routing (lines 1075-1086)
- `src/tachikoma/telegram/__init__.py:848-881` — `_handle_message` method to modify for reply-to detection
- `src/tachikoma/telegram/__init__.py:923-977` — `_handle_media` method to modify for reply-to detection
- `src/tachikoma/telegram/__init__.py:820-846` — `_detect_command` for understanding command precedence handling
- `src/tachikoma/message.py` — TextMessage dataclass showing `target_session_id: str | None = None` field (DES-014 dual declaration)
- `src/tachikoma/sessions/registry.py:506-530` — `find_session_by_external_id` used for session lookup
- DES-009 (channel delivery serialization) — delivery lock patterns for busy/idle channel branching, `enqueue_deferred` for deferred routing
- DES-013 (typed envelope with property hooks) — envelope property hooks for uniform `target_session_id` access without isinstance checks
- DES-014 (generic routing fields) — dual-declaration pattern for `target_session_id` field on all envelope subtypes
- `tests/telegram/test_channel.py` — existing Telegram channel test patterns to follow for handler testing and delivery lock coordination
- `tests/telegram/test_reactions.py` — reaction handler tests showing session routing test patterns (mock setup for `find_session_by_external_id`, `get_active_session`, envelope assertions)
- Follow `katachi:research-docs` for aiogram 3.x Message.reply_to_message accessor to verify the API before writing code

### Out of Scope
- Coordinator changes (completed in Batch 2)
- Registry changes (completed in Batch 1)
- REPL channel changes (reply-to is Telegram-only per spec)
- Other channels

### Steps

#### Step 7: Add `_extract_reply_target` helper

- Modify: `src/tachikoma/telegram/__init__.py` (add method to TelegramChannel class, near `_detect_command`)
- What to do: Add a private method:

  ```python
  def _extract_reply_target(self, message: Message) -> str | None:
      """Extract the replied-to message ID if present.

      Returns str(message.reply_to_message.message_id) when the
      message is a reply, None otherwise.
      """
      if message.reply_to_message is not None:
          return str(message.reply_to_message.message_id)
      return None
  ```

- Test: `just test tests/telegram/test_channel.py -k "extract_reply"`

#### Step 8: Add `_resolve_reply_target` session lookup helper

- Modify: `src/tachikoma/telegram/__init__.py` (add method to TelegramChannel class, near `_extract_reply_target`)
- What to do: Add a private async method mirroring the reaction handler's session lookup pattern (lines 1058-1073):

  ```python
  async def _resolve_reply_target(self, reply_to_id: str) -> str | None:
      """Look up whether a replied-to message maps to a different session.

      Returns target_session_id if the reply maps to a different session,
      None otherwise. Mirrors reaction handler session lookup pattern.
      """
      registry = self._coordinator._registry
      if registry is None:
          return None
      try:
          target_id = await registry.find_session_by_external_id(
              "telegram", reply_to_id
          )
          active = await registry.get_active_session()
          if target_id is not None and active is not None and target_id != active.id:
              return target_id
      except Exception:
          _log.exception("Session lookup for reply-to failed (best-effort)")
      return None
  ```

  Verify: returns target_session_id when lookup result differs from active session, returns None when same session or lookup returns None, calls `find_session_by_external_id("telegram", reply_to_id)` with correct args, returns None on registry errors.
- Test: `just test tests/telegram/test_channel.py -k "resolve_reply"` Modify `_handle_message` for reply-to routing

- Modify: `src/tachikoma/telegram/__init__.py:848-881`
- What to do: Restructure `_handle_message` to integrate reply-to detection:
  1. After `_detect_command`, call `_extract_reply_target(message)` → `reply_target`
  2. If `reply_target` is not None: call `await self._resolve_reply_target(reply_target)` → `target_session_id`
  3. Build envelope with `target_session_id` set when resolved (and `force_new=False` when reply-to overrides, per S5)
  4. Busy-channel path (`self._delivery_lock.locked()`):
     - If `target_session_id` is set: `enqueue_deferred(envelope)` (reply targets different session, defer for later session switch)
     - Else if command: `enqueue_deferred(envelope)` (existing behavior)
     - Else: `enqueue(envelope)` (steering, existing behavior)
  5. Idle path (inside `async with self._delivery_lock`):
     - If `target_session_id` is set: `enqueue_deferred(envelope)` (mirrors reaction handler idle path at line 1076-1082)
     - Else: `enqueue(envelope)` (existing behavior)
     - Then `_process_through_coordinator()` + `_record_incoming_external_id()` + `_drain_deferred_queue()` (existing)
- Test: `just test tests/telegram/test_channel.py -k "handle_message"`

#### Step 10: Modify `_handle_media` for reply-to routing

- Modify: `src/tachikoma/telegram/__init__.py:923-977`
- What to do: Add reply-to detection to the media handler, same pattern as Step 9 but for media messages:
  1. After building the description (line 954-959), call `_extract_reply_target(message)` → `reply_target`
  2. If `reply_target` is not None: call `await self._resolve_reply_target(reply_target)` → `target_session_id`
  3. Build TextMessage with `target_session_id` set when resolved
  4. Busy-channel path: if `target_session_id` set → `enqueue_deferred(envelope)`, else → `enqueue(envelope)` (existing behavior)
  5. Idle path: if `target_session_id` set → `enqueue_deferred(envelope)`, else → `enqueue(envelope)`. Then existing processing flow.
- Test: `just test tests/telegram/test_channel.py -k "handle_media"`

#### Step 11: Add tests for reply-to routing

- Create: `tests/telegram/test_channel.py` (new test class, e.g. `TestReplyToRouting`)
- What to do: Add comprehensive tests covering all acceptance criteria:
  - **Reply detection (R1, R2, R3)**:
    - `_extract_reply_target` returns `str(message_id)` when `reply_to_message` is set
    - `_extract_reply_target` returns `None` when `reply_to_message` is None
    - `_resolve_reply_target` returns target session ID when reply maps to different session
    - `_resolve_reply_target` returns `None` when reply maps to same session
    - `_resolve_reply_target` returns `None` when reply maps to unknown message
    - `_resolve_reply_target` returns `None` on registry errors (best-effort)
  - **Session routing (R4, R5, R6)**:
    - Reply to different session → `target_session_id` set on envelope
    - Reply to current session → no `target_session_id`, normal routing
    - Reply to unknown message → no `target_session_id`, normal routing
  - **Busy-channel (R8, R9)**:
    - Busy + different session target → `enqueue_deferred` called with `target_session_id` set
    - Busy + same session → `enqueue` called (steering)
    - Busy + unknown session → `enqueue` called (steering)
  - **Command precedence (R interaction)**:
    - Reply + `/new` → `target_session_id` set, `force_new=False`
    - Reply + `/queue` → `target_session_id` set, message deferred
  - **Media reply-to (R2)**:
    - Media message with `reply_to_message` → session lookup + `target_session_id` set
    - Media busy + different session → `enqueue_deferred` with `target_session_id`
  - Use the same mock patterns from `test_reactions.py` for session lookup and from existing `test_channel.py` for delivery lock coordination
- Test: `just test tests/telegram/test_channel.py -k "reply_to"`

#### Step 12: Verify all acceptance criteria

- Run: `just check` (lint + typecheck + all tests)
- Verify each acceptance criterion from the spec:
  - [ ] R1: Text reply detection extracts `message.reply_to_message.message_id`
  - [ ] R2: Media reply detection extracts `message.reply_to_message.message_id`
  - [ ] R3: `find_session_by_external_id("telegram", reply_id)` returns session ID
  - [ ] R4: `target_session_id` set when reply maps to different session
  - [ ] R5: Reply to current session → normal message (no switch)
  - [ ] R6: Unknown message → normal routing through boundary detection
  - [ ] R7: Stale session → Status event, no session close, fall through to normal routing
  - [ ] R8: Busy + different session → `enqueue_deferred` with `target_session_id`
  - [ ] R9: Busy + same/unknown → normal steering
  - [ ] Reply-to overrides `/new` and `/queue` commands

---

## Files Changed

- [ ] `src/tachikoma/sessions/registry.py` — Add `can_reopen_session` validation method
- [ ] `src/tachikoma/coordinator.py` — Modify `_route_to_target_session` for pre-validation, update `send_message` to handle failure and yield Status
- [ ] `src/tachikoma/telegram/__init__.py` — Add `_extract_reply_target` and `_resolve_reply_target` helpers, modify `_handle_message` and `_handle_media` for reply-to routing
- [ ] `tests/sessions/test_registry.py` — Add `TestSessionRegistryCanReopenSession` test class
- [ ] `tests/test_coordinator.py` — Update `_make_mock_registry`, update existing target_session_id tests, add new validation failure tests
- [ ] `tests/telegram/test_channel.py` — Add `TestReplyToRouting` test class

## Notes

> **Batch status markers**: ⧗ Pending → ⧗ Implementing → ✓ Done
> Updated by the implementing agent as it progresses through batches.

> **Context & Research entries**: Each entry explains *why* it's needed. Mix of project docs
> (specs, designs, ADRs, DES), code files, and external research pointers (library docs, patterns
> to investigate). These are a starting point — the implementing agent adapts them as it goes
> (removing irrelevant ones, adding discovered files, updating references).

> **Code snippets in steps**: Use snippets to show *where* code goes and *what* it should do
> (via comments), not the complete implementation. The implementation agent has full access to
> spec, design, and decision documents — plans provide structural guidance, not code.
