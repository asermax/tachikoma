# Implementation Plan: DLT-073 - Block Claude Code built-in cron tools in default config

**Delta Spec**: [../delta-specs/DLT-073.md](../delta-specs/DLT-073.md)
**Delta Design**: [../delta-designs/DLT-073.md](../delta-designs/DLT-073.md)

## Batch 1: Default value update and tests — ✓ Done
Depends on: — (none)

### Context & Research
- `docs/delta-specs/DLT-073.md` (AC section) — defines the four acceptance criteria to satisfy
- `docs/delta-designs/DLT-073.md` (Design Overview + Shape S1) — confirms single-field default value change, no new code paths
- `src/tachikoma/config.py:56-59` — current `disallowed_tools` field definition with default `["AskUserQuestion"]`
- `tests/test_config.py:56-60` — test `test_default_agent_disallowed_tools` asserting default is `["AskUserQuestion"]`
- `tests/test_config.py:229-237` — test `test_generated_file_contains_disallowed_tools` asserting generated config contains `"AskUserQuestion"`
- `tests/test_coordinator.py:204-215` — test `test_passes_disallowed_tools_to_sdk` (read-only reference; tests explicit override, not defaults — no change needed)
- `docs/feature-specs/configuration/config-system.md:39` — AC listing `agent.disallowed_tools` defaults (reconciliation target, not changed in this plan)
- `docs/feature-designs/configuration/config-system.md:85` — Modeling tree showing `disallowed_tools` default (reconciliation target, not changed in this plan)

### Steps

#### Step 1: Update default value and tests

- Modify: `src/tachikoma/config.py`, `tests/test_config.py`
- What to do:
  - In `config.py`: change the `default` parameter of `AgentSettings.disallowed_tools` from `["AskUserQuestion"]` to `["AskUserQuestion", "CronCreate", "CronDelete", "CronList"]`
  - In `test_config.py` / `test_default_agent_disallowed_tools`: update the assertion to expect the four-element list
  - In `test_config.py` / `test_generated_file_contains_disallowed_tools`: add assertions for `"CronCreate"`, `"CronDelete"`, and `"CronList"` in the generated config content
  - In `test_config.py`: add a new test `test_explicit_disallowed_tools_override_excludes_cron` that writes a minimal TOML with `[agent]\ndisallowed_tools = ["AskUserQuestion"]`, loads it via `load_settings`, and asserts the result equals only `["AskUserQuestion"]` (proves AC3/R2)
- Test: `just test tests/test_config.py -k "disallowed"` — all three tests should pass

```python
# In src/tachikoma/config.py, AgentSettings class:

# Update the default list to include cron tools
# disallowed_tools: list[str] = Field(
#     default=["AskUserQuestion", "CronCreate", "CronDelete", "CronList"],
#     ...
# )
```

#### Step 2: Verify all acceptance criteria

- Run: `just check` to confirm lint + typecheck + all tests pass
- Manual verification:
  - [ ] AC1 (R0/R1): `Settings().agent.disallowed_tools` contains all four tools — covered by `test_default_agent_disallowed_tools`
  - [ ] AC2 (R0/R1): Coordinator passes default through to SDK — covered by existing flow (`config.py` → `__main__.py:166` → Coordinator → SDK); `test_passes_disallowed_tools_to_sdk` confirms the mechanism
  - [ ] AC3 (R2): Explicit override takes precedence — covered by `test_explicit_disallowed_tools_override_excludes_cron`
  - [ ] AC4 (R3): Generated config contains all four tools — covered by `test_generated_file_contains_disallowed_tools`

---

## Files Changed

- [ ] `src/tachikoma/config.py` — Update `disallowed_tools` default value
- [ ] `tests/test_config.py` — Update default and generated config assertions, add override test

## Notes

- The coordinator test (`test_passes_disallowed_tools_to_sdk`) passes an explicit list, not the default. It validates the mechanism (config → SDK passthrough), not the specific default value. No change needed.
- Feature doc reconciliation (config-system spec and design) is handled post-implementation via `/katachi:reconcile-delta`.
