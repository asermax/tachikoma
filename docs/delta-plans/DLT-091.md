# Implementation Plan: DLT-091

**Delta Spec**: [../delta-specs/DLT-091.md](../delta-specs/DLT-091.md)
**Delta Design**: [../delta-designs/DLT-091.md](../delta-designs/DLT-091.md)
**Status**: Not Started

## Batch 1: Notification Module — ⧗ Pending
Depends on: — (none)

### Context & Research
- `docs/design/DES-006-sdk-mcp-tool-server-factory.md` — MCP tool server factory pattern that `create_notification_server()` must follow
- `docs/architecture/ADR-009-event-bus.md` — `Notification` must be a `BaseEvent[None]` subclass with typed fields
- `src/tachikoma/tasks/events.py` — Current `TaskNotification` structure (prompt, source_task_id, severity fields) to understand what's being replaced
- `src/tachikoma/tasks/executor.py:59-71` — Existing prompt templates (`NOTIFICATION_PROMPT`, `NOTIFICATION_ERROR_PROMPT`) to inform `build_notification_prompt()` design
- `katachi:research-docs` — Verify current `bubus` API: confirm `BaseEvent[T]` generic usage, `dispatch()` async signature, and whether events need explicit registration

### Steps

#### Step 1: Create notifications module with event type, prompt builder, dispatch, and MCP tool factory

- Create: `src/tachikoma/notifications.py`
- What to do:
  - Define `Notification(BaseEvent[None])` with fields: `prompt: str`, `source_id: str | None = None`, `severity: Literal["info", "error"] = "info"`
  - Define `build_notification_prompt(source: str, content: str) -> str` that produces a generic template with source, timestamp, and content
  - Define `dispatch_notification(bus: EventBus, source: str, content: str, severity: Literal["info", "error"], source_id: str | None = None) -> None` that calls `build_notification_prompt` and dispatches a `Notification` event
  - Define `create_notification_server(bus: EventBus, source: str, source_id: str) -> McpSdkServerConfig` following DES-006 factory pattern with:
    - `SendNotificationArgs(BaseModel)` with `message: str` field
    - Extracted async handler `handle_send_notification` that validates non-empty message and calls `dispatch_notification()`
    - `send_notification` tool registered inside factory
- Test: `just typecheck`

#### Step 2: Write tests for notifications module

- Create: `tests/test_notifications.py`
- What to do:
  - Test `build_notification_prompt` produces expected template with source, timestamp, content
  - Test `dispatch_notification` dispatches `Notification` event with correct fields (prompt, source_id, severity)
  - Test `send_notification` tool handler returns error for empty message
  - Test `send_notification` tool handler dispatches notification and returns success for valid message
  - Test `Notification` event construction (info and error severity, defaults)
- Test: `just test tests/test_notifications.py`

---

## Batch 2: Model, Repository & Tools Cleanup — ⧗ Pending
Depends on: — (none)

### Context & Research
- `src/tachikoma/tasks/model.py:103,147,160` — `notify` field on `TaskDefinition` dataclass, `TaskDefinitionRecord` ORM, `to_domain()`
- `src/tachikoma/tasks/repository.py:51,128` — `create_definition` record construction passes `notify`, `update_definition` docstring lists accepted fields including `notify`
- `src/tachikoma/tasks/tools.py:42,52,120,144-146,216,267,313-314` — `notify` in `CreateTaskArgs`, `UpdateTaskArgs`, `list_tasks` display, `create_task` description/construction, `update_task` description/update dict
- `src/tachikoma/database.py:78-172` — `_run_migrations()` pragma-based pattern for column checks; add reverse (drop column)
- `tests/tasks/conftest.py:28,39` — `_make_definition` helper accepts and passes `notify` parameter
- `tests/tasks/test_model.py:153,234,246` — References `notify` in `TaskDefinition` construction and ORM-to-domain assertion
- `tests/tasks/test_tools.py` — Tests for MCP tools (some reference `notify` in args validation)
- `tests/test_database.py:91` — `"notify"` in `expected_defs` set that validates `task_definitions` schema columns

### Steps

#### Step 3: Remove notify from TaskDefinition and ORM model

- Modify: `src/tachikoma/tasks/model.py`
- What to do:
  - Remove `notify: str | None = None` from `TaskDefinition` dataclass (line 103)
  - Remove `notify: Mapped[str | None]` from `TaskDefinitionRecord` ORM model (line 147)
  - Remove `notify=self.notify` from `to_domain()` (line 160)
- Test: `just typecheck`

#### Step 4: Remove notify from repository

- Modify: `src/tachikoma/tasks/repository.py`
- What to do:
  - Remove `notify=definition.notify` from `create_definition` record construction (line 51)
  - Remove `notify` from `update_definition` docstring accepted fields (line 128)
- Test: `just typecheck`

#### Step 5: Remove notify from MCP tools

- Modify: `src/tachikoma/tasks/tools.py`
- What to do:
  - Remove `notify: str | None = None` from `CreateTaskArgs` (line 42)
  - Remove `notify: str | None = None` from `UpdateTaskArgs` (line 52)
  - Remove `if d.notify:` block from `list_tasks` display (lines 120-121)
  - Update `create_task` tool description: remove `notify` parameter documentation (lines 144-146, the lines containing "notify (str, optional)" and "notify regardless")
  - Remove `notify=parsed.notify` from `TaskDefinition` construction in `create_task` (line 216)
  - Update `update_task` tool description: remove `notify` parameter line (line 267)
  - Remove `if parsed.notify is not None: updates["notify"] = parsed.notify` from `update_task` handler (lines 313-314)
- Test: `just test tests/tasks/test_tools.py`

#### Step 6: Add database migration to drop notify column and update schema test

- Modify: `src/tachikoma/database.py`, `tests/test_database.py`
- What to do:
  - In `database.py`, in `_run_migrations()`, add a pragma check after existing migrations:
    ```python
    # Check if notify column exists on task_definitions table (removed in DLT-091)
    result = await conn.execute(
        text("SELECT * FROM pragma_table_info('task_definitions') WHERE name='notify'")
    )
    if result.fetchone() is not None:
        await conn.execute(text("ALTER TABLE task_definitions DROP COLUMN notify"))
        _log.info("Schema migration: dropped 'notify' column from task_definitions table")
    ```
  - In `tests/test_database.py`, remove `"notify"` from the `expected_defs` set (line 91)
- Test: `just test tests/test_database.py`

#### Step 7: Update test helpers and fix all broken model/tools tests

- Modify: `tests/tasks/conftest.py`, `tests/tasks/test_model.py`, `tests/tasks/test_tools.py`
- What to do:
  - Remove `notify` parameter from `_make_definition` in `conftest.py` (lines 28, 39)
  - In `tests/tasks/test_model.py`:
    - Remove `notify=None` from `TestTaskDefinition.test_create_definition` construction (line 153)
    - Remove `notify="Tell user"` from `TestORMModels.test_definition_record_to_domain` record construction (line 234)
    - Remove `assert domain.notify == "Tell user"` assertion (line 246)
  - Fix any test in `test_tools.py` that references `notify` on `CreateTaskArgs` or `UpdateTaskArgs`
- Test: `just test tests/tasks/`

---

## Batch 3: Executor Refactor & Notification Server Integration — ⧗ Pending
Depends on: Batch 1, Batch 2

### Context & Research
- `src/tachikoma/tasks/executor.py` — Full refactor target
  - Lines 25, 32: imports of `fork_and_capture` and `TaskNotification`
  - Lines 59-71: `NOTIFICATION_PROMPT` and `NOTIFICATION_ERROR_PROMPT` constants
  - Lines 259-262: definition fetch (currently for `notify` field)
  - Lines 272-288: SDK options construction — add notification MCP server here
  - Lines 333-343: Success path calls `_dispatch_notification` — remove this call
  - Lines 345-356: Stuck path calls `_dispatch_notification` — replace with `dispatch_notification()`
  - Lines 362-376: Max iterations path calls `_dispatch_notification` — replace with `dispatch_notification()`
  - Lines 383-396: Exception path calls `_dispatch_notification` — replace with `dispatch_notification()`
  - Lines 572-689: `_dispatch_notification()` and `_generate_notification()` — remove entirely
- Batch 1 output: `notifications.py` with `dispatch_notification()` and `create_notification_server()`
- Batch 2 output: `TaskDefinition` without `notify` field
- `tests/tasks/test_executor.py` — Existing tests need major rewrite (notification tests change fundamentally)

### Steps

#### Step 8: Refactor executor — remove old notification code and add new dispatch integration

- Modify: `src/tachikoma/tasks/executor.py`
- What to do:
  - Remove import of `fork_and_capture` from `post_processing` (line 25)
  - Remove import of `TaskNotification` from `tasks.events` (line 32)
  - Add import of `dispatch_notification` and `create_notification_server` from `tachikoma.notifications`
  - Remove `NOTIFICATION_PROMPT` and `NOTIFICATION_ERROR_PROMPT` constants (lines 59-71)
  - Remove `_generate_notification()` method entirely (lines 644-689)
  - Remove `_dispatch_notification()` method entirely (lines 572-643)
  - In `execute()`, build notification source string after getting definition:
    ```python
    # Build notification context for failure dispatch
    notification_source = (
        f"Background task: {definition.name}" if definition
        else f"Background task: {instance.prompt[:100]}"
    )
    ```
  - Before building SDK options, create notification server and add to mcp_servers:
    ```python
    notification_server = create_notification_server(
        self._bus, notification_source, instance.id,
    )
    # Add to preprocessing_result.mcp_servers
    preprocessing_result.mcp_servers["notifications"] = notification_server
    ```
  - Success path (status == "complete"): remove the `_dispatch_notification` call entirely — agent controls notifications via `send_notification`
  - Stuck path: replace `_dispatch_notification(...)` with `dispatch_notification(self._bus, notification_source, f"Task failed: {feedback}", "error", instance.id)`
  - Max iterations path: replace with `dispatch_notification(self._bus, notification_source, f"Task failed: reached max iterations ({max_iterations})", "error", instance.id)`
  - Exception path: replace with `dispatch_notification(self._bus, notification_source, f"Task failed with error: {exc}", "error", instance.id)`
  - CancelledError path: keep unchanged (no notification on cancel)
  - Remove the comment `# Get the definition if available (for notify field)` (line 259) — definition is still fetched for notification source naming
- Test: `just typecheck`

#### Step 9: Rewrite executor notification tests

- Modify: `tests/tasks/test_executor.py`
- What to do:
  - Replace import of `TaskNotification` with `Notification` from `tachikoma.notifications`
  - Remove entire `TestNotificationGeneration` class (lines 417-773: all fork-based tests are no longer relevant)
  - Remove `test_no_notification_when_notify_null` (line 300-363): this tested the old `notify` field null behavior; rename and replace with `test_no_notification_on_success` that verifies NO notification is dispatched when evaluator returns complete (agent-driven model)
  - Update `test_failure_dispatches_error_notification`: assert `dispatched_events[0]` is `Notification` (not `TaskNotification`), verify severity is "error", check prompt contains error content
  - Update `test_max_iterations_marks_failed`: verify dispatched event is `Notification` with severity "error"
  - Add new test `test_notification_server_registered_in_sdk_options`: verify the MCP server is present in the SDK options' `mcp_servers` dict
  - Add new test `test_agent_failure_and_agent_notification_both_delivered`: simulate a scenario where `send_notification` was called during execution (dispatch a Notification) then the task fails — verify both the agent notification and the failure notification are dispatched independently (validates R7)
- Test: `just test tests/tasks/test_executor.py`

---

## Batch 4: Channel Subscribers & Event Migration — ⧗ Pending
Depends on: Batch 1, Batch 3

### Context & Research
- `src/tachikoma/telegram.py:34,408,441,625-643` — imports `TaskNotification`, class docstring mentions subscribed event types, subscribes via `bus.on()`, handler `_handle_notification` with type annotation
- `src/tachikoma/repl.py:18,63-68,80,107,155-156,203-227` — imports, class docstring mentions subscribed event types, queue type `asyncio.Queue[SessionTaskReady | TaskNotification]`, subscription, isinstance check, handlers
- `src/tachikoma/tasks/__init__.py:8,14,33` — module docstring mentions `TaskNotification`, re-exports `TaskNotification`
- `src/tachikoma/tasks/events.py:32-51` — `TaskNotification` class to remove
- `src/tachikoma/context/loading.py:210-218` — Preamble references `notify` parameter in create_task and update_task descriptions
- `tests/tasks/test_events.py:7,53-97` — `TestTaskNotification` class tests the event type
- Note: bubus event dispatch is type-based (subscribe by class). The bus does not persist event history by default — no migration needed for stored event references. If WAL or history middleware is enabled in the future, a migration would be needed then.

### Steps

#### Step 10: Update telegram channel subscriber

- Modify: `src/tachikoma/telegram.py`
- What to do:
  - Replace import of `TaskNotification` from `tasks.events` with import of `Notification` from `tachikoma.notifications`
  - Update `TelegramChannel` class docstring: change `TaskNotification` mention to `Notification`
  - Update `bus.on(TaskNotification, ...)` to `bus.on(Notification, ...)`
  - Update `_handle_notification` type annotation from `TaskNotification` to `Notification`
  - Update handler body: `event.source_task_id` → `event.source_id`
- Test: `just typecheck`

#### Step 11: Update REPL channel subscriber

- Modify: `src/tachikoma/repl.py`
- What to do:
  - Replace import of `TaskNotification` from `tasks.events` with import of `Notification` from `tachikoma.notifications`
  - Update `Repl` class docstring: change `TaskNotification` mention to `Notification`
  - Update queue type annotation from `asyncio.Queue[SessionTaskReady | TaskNotification]` to `asyncio.Queue[SessionTaskReady | Notification]`
  - Update `bus.on(TaskNotification, ...)` to `bus.on(Notification, ...)`
  - Update isinstance check from `TaskNotification` to `Notification`
  - Update `_handle_notification` and `_execute_notification` type annotations to `Notification`
  - Update handler bodies: `event.source_task_id` → `event.source_id`
- Test: `just typecheck`

#### Step 12: Remove TaskNotification from tasks package

- Modify: `src/tachikoma/tasks/events.py`, `src/tachikoma/tasks/__init__.py`
- What to do:
  - Remove `TaskNotification` class from `events.py` (lines 32-51)
  - Remove `TaskNotification` from imports and `__all__` in `__init__.py` (lines 14, 33)
  - Update module docstring in `__init__.py` (line 8): remove `TaskNotification` from the listed event classes, keep `SessionTaskReady`
- Test: `just typecheck`

#### Step 13: Update preamble to remove notify references and add send_notification

- Modify: `src/tachikoma/context/loading.py`
- What to do:
  - In the Tasks section of `SYSTEM_PREAMBLE`:
    - Remove the `notify` parameter documentation from `create_task` description (the lines mentioning "notify (str, optional): Success notification instruction...")
    - Remove `notify` from `update_task` updatable fields list
    - Add to the tool descriptions: mention that `send_notification` is available during background task execution for delivering messages to the user, and that failures are auto-notified
- Test: `just test tests/context/`

#### Step 14: Update event tests

- Modify: `tests/tasks/test_events.py`
- What to do:
  - Remove `TestTaskNotification` class entirely (lines 53-97)
  - Remove `TaskNotification` from import (line 7) — replaced by `Notification` tests in `test_notifications.py` (Batch 1)
- Test: `just test tests/tasks/test_events.py`

---

## Batch 5: System Prompt & Final Validation — ⧗ Pending
Depends on: Batch 1, Batch 2, Batch 3, Batch 4

### Context & Research
- `src/tachikoma/tasks/executor.py:77-79` — `BACKGROUND_TASK_SYSTEM_PROMPT` needs `send_notification` guidance
- `docs/delta-specs/DLT-091.md` — Acceptance criteria for validation pass

### Steps

#### Step 15: Update background task system prompt

- Modify: `src/tachikoma/tasks/executor.py`
- What to do:
  - Update `BACKGROUND_TASK_SYSTEM_PROMPT` to add guidance about `send_notification`:
    - Explain the tool is available for delivering notifications to the user
    - Guide the agent to use it when results are meaningful (not for every task)
    - Note that failures are automatically notified
- Test: `just test tests/tasks/test_executor.py`

#### Step 16: Run full test suite and validate acceptance criteria

- What to do:
  - Run `just check` (lint + typecheck + test)
  - Verify each acceptance criterion from the spec:
    - R1: `notify` field gone from model, tools, database schema
    - R2: No success notification dispatch in executor
    - R3: Failure notifications dispatch via `dispatch_notification()`
    - R4: `send_notification` tool available during background execution
    - R5: Prompt template consistent for both agent-driven and failure
    - R6: Preamble and system prompt updated
    - R7: Multiple notifications supported (each dispatch is independent)
- Test: `just check`

## Files Changed

- [ ] `src/tachikoma/notifications.py` — New module: Notification event, prompt builder, dispatch function, MCP tool factory
- [ ] `tests/test_notifications.py` — Tests for notifications module
- [ ] `src/tachikoma/tasks/model.py` — Remove `notify` from TaskDefinition and TaskDefinitionRecord
- [ ] `src/tachikoma/tasks/repository.py` — Remove `notify` from create/update paths
- [ ] `src/tachikoma/tasks/tools.py` — Remove `notify` from CreateTaskArgs, UpdateTaskArgs, list output, tool descriptions
- [ ] `src/tachikoma/database.py` — Add migration to drop `notify` column
- [ ] `tests/test_database.py` — Remove `notify` from expected schema columns
- [ ] `src/tachikoma/tasks/executor.py` — Remove fork-based notification, add notification server registration, update failure dispatch, update system prompt
- [ ] `src/tachikoma/tasks/events.py` — Remove `TaskNotification` class
- [ ] `src/tachikoma/tasks/__init__.py` — Remove `TaskNotification` re-export, update docstring
- [ ] `src/tachikoma/telegram.py` — Import `Notification`, update subscriber and docstring
- [ ] `src/tachikoma/repl.py` — Import `Notification`, update subscriber, queue type, and docstring
- [ ] `src/tachikoma/context/loading.py` — Update preamble Tasks section
- [ ] `tests/tasks/conftest.py` — Remove `notify` from `_make_definition`
- [ ] `tests/tasks/test_model.py` — Remove `notify` from test assertions and construction
- [ ] `tests/tasks/test_events.py` — Remove `TestTaskNotification` class
- [ ] `tests/tasks/test_executor.py` — Rewrite notification tests for new model
- [ ] `tests/tasks/test_tools.py` — Remove `notify` from test cases

## Notes

> **Batch status markers**: ⧗ Pending → ⧗ Implementing → ✓ Done
> Updated by the implementing agent as it progresses through batches.

> **File-level dependency**: `tests/tasks/conftest.py` is modified in Batch 2 (Step 7) and consumed by Batch 3 (Step 9). This is correctly ordered since Batch 3 depends on Batch 2.
